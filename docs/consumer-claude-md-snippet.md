# Tale UI — CLAUDE.md snippet for consuming projects

Copy the section below into your project's `CLAUDE.md` file.

---

## UI Components (@tale-ui/react)

This project uses `@tale-ui/react` for all UI components.

Before generating or modifying component code, you MUST:

1. **Read the setup guide** in `node_modules/@tale-ui/react/README.md` — it contains critical configuration (font-size base, style imports, dark mode, theme overrides) that will produce broken output if skipped.

2. **Check the JSDoc `@example` on each component's .d.ts export** before using it. Every component's Root export includes a `@example` block showing the correct import path, sub-parts, and composition pattern. Read the `.d.ts` file for each component you intend to use:

   ```text
   node_modules/@tale-ui/react/esm/{name}/{Name}.styled.d.ts
   ```

   > **Exception:** A few components use `{Name}.d.ts` (not `.styled.d.ts`): `CSPProvider`, `CheckboxGroup`, `RadioGroup`, `Container`, and `mergeProps`.

   The `@example` block at the top of each file is the authoritative usage reference.

3. **For deeper details** (all props, all variants, advanced patterns), read the local component doc:

   ```text
   node_modules/@tale-ui/react/docs/{name}.md
   ```

4. **Do not guess component APIs.** Always check the `@example` block first. Incorrect usage (wrong sub-part names, missing wrapper components, wrong import paths) causes build failures.

5. **Common pitfalls to avoid:**
   - **`Radio` and `RadioGroup` are separate components with separate imports, and `RadioGroup` has NO sub-parts** — `Radio` is a namespace component (`Radio.Root`, `Radio.Indicator`) imported from `@tale-ui/react/radio`; `RadioGroup` is a standalone simple component imported from `@tale-ui/react/radio-group`. `RadioGroup` has **no** `.Radios`, `.Item`, `.Indicator`, or any other sub-part — using `<RadioGroup.Radios>`, `<RadioGroup.Item>`, or `<RadioGroup.Indicator>` will cause 'Property does not exist' TypeScript errors. There is also NO `Radio.Group` sub-part — using `<Radio.Group>` will fail at runtime. The correct pattern is `<RadioGroup label="...">` directly wrapping `<Radio.Root>` children (with `<Radio.Indicator />` inside each `Radio.Root`). Example: `import { Radio } from '@tale-ui/react/radio'; import { RadioGroup } from '@tale-ui/react/radio-group';` then `<RadioGroup label="Select a plan"><Radio.Root value="free"><Radio.Indicator />Free</Radio.Root><Radio.Root value="pro"><Radio.Indicator />Pro</Radio.Root><Radio.Root value="enterprise"><Radio.Indicator />Enterprise</Radio.Root></RadioGroup>`.
   - **Namespace components** (use `<Component.Root>`, never `<Component>` directly): `Accordion`, `AlertDialog`, `Autocomplete` (**requires `npm install react-aria` for useFilter — see useFilter pitfall below**), `Avatar`, `Banner`, `Breadcrumbs`, `Calendar`, `Card`, `Carousel`, `Checkbox`, `ColorArea`, `ColorField`, `ColorPicker`, `ColorSlider`, `ColorSwatchPicker`, `ColorWheel`, `Combobox`, `ContextMenu`, `DateField`, `DatePicker`, `DateRangePicker`, `Dialog`, `**`Disclosure` uses `Disclosure.Panel` for content, NOT `Disclosure.Content`** — there is no Content, Header, or Icon sub-part on the `Disclosure` namespace. The three available sub-parts are Root, Trigger, and Panel. **`Disclosure.Root` controlled-state props are isExpanded/onExpandedChange (NOT open/onOpenChange)** — passing `open={...}` causes 'is not assignable to type DisclosureRootProps' TypeScript errors. For uncontrolled usage omit both props. Correct structure: `<Disclosure.Root><Disclosure.Trigger>Show more</Disclosure.Trigger><Disclosure.Panel>Hidden content revealed on expand.</Disclosure.Panel></Disclosure.Root>`. For controlled state: `<Disclosure.Root isExpanded={isOpen} onExpandedChange={setIsOpen}>`.`, `Drawer`, `EmptyState`, `Field`, `Fieldset`, `GridList`, `Input`, `List`, `Menu`, `Menubar`, `Meter`, `NavigationMenu`, `NumberField`, `Pagination`, `PaymentInput`, `PinInput`, `Popover`, `PreviewCard`, `ProgressBar`, `ProgressCircle`, `Radio`, `RangeCalendar`, `ScrollArea`, `SearchField`, `Select`, `Slider`, `Switch`, `Table`, `Tabs`, `TagGroup`, `TextArea`, `TextField`, `TimeField`, `Toolbar` (**sub-parts:** Root, Group, Button, Link, Input, Separator — use `<Toolbar.Root aria-label="…">` as the container; `<Toolbar.Button>` for action buttons, NOT a separate ToolbarButton export; there is NO standalone ToolbarButton named export and using it causes 'has no exported member' TypeScript errors. **`Toolbar.Button` only accepts standard HTML button attributes (onClick, aria-label, disabled, etc.) — it does NOT accept isSelected, onChange, defaultSelected, or any other ToggleButton props**. Passing isSelected or onChange to `Toolbar.Button` causes 'is not assignable to type ButtonHTMLAttributes<HTMLButtonElement>' TypeScript errors. For toggleable toolbar buttons (e.g. Bold/Italic/Underline that stay pressed), use `<ToggleButton>` from `@tale-ui/react/toggle-button` inside the toolbar instead of `<Toolbar.Button>`: `import { useState } from 'react'; import { ToggleButton } from '@tale-ui/react/toggle-button';` then `const [bold, setBold] = useState(false); const [italic, setItalic] = useState(false);` and `<Toolbar.Root aria-label="Formatting"><Toolbar.Group><ToggleButton isSelected={bold} onChange={setBold} aria-label="Bold"><Icon icon={Bold} size="sm" /></ToggleButton></Toolbar.Group></Toolbar.Root>`. **Always import useState from react when managing toggle state** — omitting it causes 'Cannot find name useState' TypeScript errors: `import { useState } from 'react';`. For plain (non-toggle) action buttons, use `<Toolbar.Button onClick={...}>`. Example: `<Toolbar.Root aria-label="Formatting"><Toolbar.Group><Toolbar.Button>Bold</Toolbar.Button><Toolbar.Button>Italic</Toolbar.Button><Toolbar.Button>Underline</Toolbar.Button></Toolbar.Group></Toolbar.Root>`), `Tooltip`, `Tree`. **Simple components** (direct use, no `.Root`): `AppStoreButton`, `Badge`, `Button`, `CSPProvider`, `CheckboxGroup`, `ColorModeToggle`, `ColorSwatch`, `Column`, `Container`, `DotIcon`, `DropZone`, `FeaturedIcon` (**does NOT accept an icon prop** — it is a wrapper that takes an `<Icon>` child. Pass the icon as children: `<FeaturedIcon size="lg"><Icon icon={Bell} /></FeaturedIcon>`. You must also import both `Icon` from `@tale-ui/react/icon` and the lucide icon (e.g. Bell) from `lucide-react`. Using `<FeaturedIcon icon={Bell} />` causes 'is not assignable to type FeaturedIconProps' and 'Cannot find name Bell' TypeScript errors because there is no icon prop and the lucide icon is never imported. Correct example: `import { FeaturedIcon } from '@tale-ui/react/featured-icon'; import { Icon } from '@tale-ui/react/icon'; import { Bell } from 'lucide-react'; ... <FeaturedIcon size="lg"><Icon icon={Bell} /></FeaturedIcon>`), `FileTrigger`, `Form`, `I18nProvider`, `Icon`, `IconButton`, `Image`, `Link`, `RadioGroup`, `RatingBadge`, `RatingStars`, `Row`, `SelectNative`, `Separator`, `SocialButton`, `SocialButtonGroup`, `Spinner`. **`SelectNative` is a simple component with NO sub-parts** — it is a single `<select>` element wrapper and has no `.Root`, `.Label`, `.Trigger`, `.Value`, `.Popover`, `.ListBox`, or `.Item` sub-parts. Using any of these will cause 'Property does not exist' TypeScript errors. The correct usage is to use `<SelectNative>` directly with a plain HTML `<label>` element and standard HTML `<option>` children: `<label htmlFor="country">Country</label><SelectNative id="country" defaultValue="us"><option value="us">United States</option><option value="uk">United Kingdom</option><option value="ca">Canada</option></SelectNative>`. **There is NO `@tale-ui/react/label` module and NO Label component in Tale UI** — `import { Label } from '@tale-ui/react/label'` causes 'Cannot find module' TypeScript errors. Always use a native `<label htmlFor="...">` HTML element for labeling `SelectNative`. If you need a fully-featured custom select with a popover listbox, use the namespace Select component instead (`import { Select } from '@tale-ui/react/select'`). (**imported from `@tale-ui/react/spinner`**, NOT from `@tale-ui/react/text` — `import { Spinner } from '@tale-ui/react/spinner'`), `Text`, `ToggleButton`, `ToggleButtonGroup`, `mergeProps`. **`DropZone` has NO sub-parts** — there is no `DropZone.Content`, `DropZone.Icon`, or `DropZone.Title`. Pass children directly as arbitrary JSX (e.g. `<p>Drop files here</p>`). The onDrop callback receives a DropEvent object (NOT a FileList) — access dropped files via `e.items.filter(item => item.kind === 'file')`. Using `(fileList: FileList | null) => void` as the handler type causes 'is not assignable to type' TypeScript errors. Correct usage: `<DropZone onDrop={(e) => { const names = e.items.filter(i => i.kind === 'file').map(i => 'name' in i ? i.name : 'unknown'); }}><p>Drop files here</p></DropZone>`.
   - **`Spinner` `label` prop sets the `aria-label` — it does NOT render visible text.** For a spinner with only an accessible label, use `<Spinner label="Loading data..." />` (single self-closing element, no wrapper needed). If you need visible loading text alongside the spinner, wrap both in a parent: `<Row gap="3xs" style={{ alignItems: 'center' }}><Spinner size="sm" /><Text variant="text" size="xs" color="muted">Loading data...</Text></Row>` — do NOT render sibling JSX elements without a parent wrapper, as JSX requires a single root element.
   - `Drawer.Root` uses `open`/`onOpenChange` (NOT `isOpen`) — it is a custom component, not built on React Aria Modal. Dialog and AlertDialog use `isOpen`.
   - **`Dialog.Root` controlled-state props are `isOpen`/`onOpenChange` (NOT `open`/`onOpenChange`)** — passing `open={...}` to `Dialog.Root` causes 'is not assignable to type DialogTriggerProps' TypeScript errors. Always use `isOpen`: `<Dialog.Root isOpen={isOpen} onOpenChange={onOpenChange}>`. The same rule applies to `AlertDialog.Root` — both use `isOpen`, not `open`.
   - `Drawer.Backdrop` must be a self-closing sibling of `Drawer.Popup` (`<Drawer.Backdrop />`), never a wrapper around it.
   - There is NO `Drawer.Actions` part (unlike `Dialog.Actions`). Wrap action buttons in a plain `<div>` with flex layout instead.
   - `Meter.Value`, `ProgressBar.Value`, and `ProgressCircle.Value` need children text (e.g. `<Meter.Value>60%</Meter.Value>`, `<ProgressBar.Value>65%</ProgressBar.Value>`, `<ProgressCircle.Value>65%</ProgressCircle.Value>`), not self-closing. **`Meter.Root` uses `minValue`/`maxValue` (NOT `min`/`max`)** — passing `max={100}` or `min={0}` causes 'is not assignable to type RootProps' TypeScript errors. Use `<Meter.Root value={75} minValue={0} maxValue={100}>`. This is the React Aria convention shared by `Slider.Root`, `ProgressBar.Root`, and `NumberField.Root`. **There is NO `Meter.Output` sub-part and NO `ProgressBar.Output` sub-part** — the value display sub-part is `ProgressBar.Value`, not `Output`. Do NOT place `<Text>` inside `ProgressCircle.Root` as a child — use `<ProgressCircle.Value>` for the label. Example: `<ProgressBar.Root value={65}><ProgressBar.Header><ProgressBar.Label>Uploading</ProgressBar.Label><ProgressBar.Value>65%</ProgressBar.Value></ProgressBar.Header><ProgressBar.Track><ProgressBar.Indicator /></ProgressBar.Track></ProgressBar.Root>`. **`ProgressCircle.Root` `size` prop** only accepts `'sm'`, `'md'`, or `'lg'` — NOT the standard Tale UI token suffixes (`'s'`, `'m'`, `'l'`) and NOT `'xl'` or any other value. Using `size="xl"` causes 'Type '"xl"' is not assignable to type 'Size | undefined'' TypeScript errors. Example: `<ProgressCircle.Root value={65} size="md"><ProgressCircle.Track /><ProgressCircle.Value>65%</ProgressCircle.Value></ProgressCircle.Root>`.
   - `NumberField.Increment` and `NumberField.Decrement` render their own lucide-react icons — use them self-closing. **`NumberField.Root` `value` prop accepts `number | undefined`, NOT `number | null`** — using `useState<number | null>` causes 'Type 'number | null' is not assignable to type 'number | undefined'' TypeScript errors. For controlled state use `useState<number | undefined>(1)` or simply `useState(1)` (let TypeScript infer): `const [quantity, setQuantity] = useState(1); ... <NumberField.Root value={quantity} onChange={setQuantity}>`. **`NumberField` sub-parts are ONLY `Root`, `Label`, `Group`, `Input`, `Increment`, and `Decrement` — there is NO `NumberField.Step`, `NumberField.HintText`, or any other sub-part** — using `<NumberField.Step>` causes 'Property 'Step' does not exist' TypeScript errors. Min/max constraints are set as `minValue` and `maxValue` props directly on `NumberField.Root` (e.g. `<NumberField.Root minValue={1} maxValue={99}>`). If you need to display hint text like 'Min: 1 • Max: 99', render a plain `<Text color="muted">` element outside the `NumberField` structure. Example: `<NumberField.Root minValue={1} maxValue={99}><NumberField.Label>Quantity</NumberField.Label><NumberField.Group><NumberField.Decrement /><NumberField.Input /><NumberField.Increment /></NumberField.Group></NumberField.Root><Text color="muted">Min: 1 • Max: 99</Text>`.
   - **`ToggleButton` uses `isSelected`/`defaultSelected` for toggle state, NOT `pressed`** — passing `pressed` or the shorthand `is` (which JSX parses as `is={true}`) causes 'is not assignable to type ToggleButtonProps' TypeScript errors. For uncontrolled use `defaultSelected={false}`, for controlled use `isSelected={pressed} onChange={setPressed}`. Example: `<ToggleButton defaultSelected={false}><strong>B</strong></ToggleButton>` or controlled: `const [bold, setBold] = useState(false); ... <ToggleButton isSelected={bold} onChange={setBold}><strong>B</strong></ToggleButton>`.
   - `ToggleButtonGroup` requires `aria-label` or `aria-labelledby` for accessibility.
   - **`ToggleButtonGroup` does NOT accept `type`, `value`, or `onValueChange` props** — it is a layout/grouping container only, not a selection manager. Using `<ToggleButtonGroup type="single" value={...} onValueChange={...}>` causes 'is not assignable to type ToggleButtonGroupProps' TypeScript errors. Each `ToggleButton` inside the group manages its own selection state via `isSelected` and `onChange`. **`ToggleButton` does NOT accept a `value` prop** — using `<ToggleButton value="left">` causes 'is not assignable to type ToggleButtonProps' TypeScript errors. For a single-select toggle group (e.g. text alignment), manage a single state variable and set `isSelected` on each button: `const [alignment, setAlignment] = useState<'left' | 'center' | 'right'>('left');` then `<ToggleButtonGroup aria-label="Text alignment"><ToggleButton isSelected={alignment === 'left'} onChange={() => setAlignment('left')}><AlignLeft size={16} /></ToggleButton><ToggleButton isSelected={alignment === 'center'} onChange={() => setAlignment('center')}><AlignCenter size={16} /></ToggleButton><ToggleButton isSelected={alignment === 'right'} onChange={() => setAlignment('right')}><AlignRight size={16} /></ToggleButton></ToggleButtonGroup>`. Always import `useState` from `react`: `import { useState } from 'react';`.
   - **`ToggleButtonGroup` is exported from `@tale-ui/react/toggle-button`** — there is NO separate `@tale-ui/react/toggle-button-group` module. Import both components from the same path: `import { ToggleButton, ToggleButtonGroup } from '@tale-ui/react/toggle-button';`. Using `import { ToggleButtonGroup } from '@tale-ui/react/toggle-button-group'` will cause "Cannot find module" TypeScript errors.
   - Dark mode: always set `data-color-mode` to `"dark"` or `"light"` — never remove the attribute to switch to light mode.
   - `Calendar.PreviousButton`, `Calendar.Heading`, and `Calendar.NextButton` must be wrapped in `Calendar.Header` — it provides the flex layout. Same rule applies to `RangeCalendar.Header`.
   - `Calendar.GridHeader` passes day name strings, not dates — use `Calendar.GridHeaderCell` inside it, not `Calendar.Cell`. Reserve `Calendar.Cell` for `Calendar.GridBody`.
   - `Calendar.Heading` must have no vertical margin or padding — do not add spacing to it or wrap it in an element that adds margins (e.g. `<h2>`).
   - **There is NO `HexColorField` component** — the component for a hex (or any CSS color format) text input is `ColorField`, imported from `@tale-ui/react/color-field`. `ColorField` is a namespace component: use `<ColorField.Root>`, `<ColorField.Label>`, and `<ColorField.Input>`. Example: `<ColorField.Root defaultValue="#3b82f6"><ColorField.Label>Background color</ColorField.Label><ColorField.Input /></ColorField.Root>`. Using `import { HexColorField } from '@tale-ui/react/hex-color-field'` will cause "Cannot find module" TypeScript errors.
   - **`ColorArea` only has `Root` and `Thumb` sub-parts** — there is no `ColorArea.Gradient`, `ColorArea.Background`, or any other sub-part. The gradient background is rendered automatically by `ColorArea.Root`. Correct structure: `<ColorArea.Root value={color} onChange={setColor} xChannel="saturation" yChannel="brightness"><ColorArea.Thumb /></ColorArea.Root>`. Using `<ColorArea.Gradient>` will cause "Property 'Gradient' does not exist" TypeScript errors.
   - Do NOT nest `ColorSlider` inside `ColorPicker.Root` — the context does not propagate and causes "Unknown color channel: hue" at runtime. Use `ColorArea` and `ColorSlider` as standalone components with shared `useState`. **`ColorSlider` is a namespace component — always use `<ColorSlider.Root>`, never `<ColorSlider>` directly** (same rule as `ColorArea.Root`). **Both `ColorArea.Root` and `ColorSlider.Root` accept a full `Color` object for `value` and return a `Color` object via `onChange` — do NOT extract a numeric channel (e.g. `color.hue`) and pass it as the `value`; do NOT write a separate handler that calls `parseColor` to reconstruct the color from a number.** **Do NOT initialize color state with a plain JS object like `useState({ h: 200, s: 100, v: 100, alpha: 1 })` — this causes 'Type '{ h: number; s: number; v: number; alpha: number; }' is not assignable to type 'string | Color | undefined'' TypeScript errors because `value` requires a `Color` instance, not a POJO.** The correct shared-state pattern is a single `const [color, setColor] = useState(parseColor('hsl(0, 100%, 50%)'))` and then `value={color} onChange={setColor}` on both components: `<ColorArea.Root value={color} onChange={setColor} xChannel="saturation" yChannel="brightness"><ColorArea.Thumb /></ColorArea.Root>` and `<ColorSlider.Root value={color} onChange={setColor} channel="hue"><ColorSlider.Track><ColorSlider.Thumb /></ColorSlider.Track></ColorSlider.Root>`. You must run `npm install @internationalized/color` as a **separate prerequisite step before writing any code** that imports `parseColor` — missing this package causes "Cannot find module '@internationalized/color'" TypeScript errors. **When generating any code that imports from `@internationalized/color` (i.e. any controlled color state using `parseColor` or the `Color` type), you MUST always output the install command as an explicit prerequisite at the top of your response, e.g.: `npm install @internationalized/color`. Do not silently write the import and leave the user to discover the missing package. Additionally, add `// Prerequisite: npm install @internationalized/color` as the very first line of every generated file that contains `import ... from '@internationalized/color'`, so the install requirement is visible even when only the code is shown without surrounding text. **Note: L1 TypeScript validation will always report 'Cannot find module \"@internationalized/color\"' for this import when the package is not installed as a direct project dependency — this is an expected prerequisite-not-installed error, NOT a code generation mistake. Generated code that includes the `// Prerequisite` comment and otherwise follows the rules below is correct; the L1 error resolves once the user runs `npm install @internationalized/color`.** **The `Color` type must be imported from `@internationalized/color`, NOT from `react`** — writing `import { useState, type Color } from 'react'` causes 'Module '"react"' has no exported member 'Color'' TypeScript errors. Always use two separate imports: `import { useState } from 'react';` and `import { parseColor, type Color } from '@internationalized/color';` (requires `npm install @internationalized/color` as a prerequisite). **If wrapping them in `<Column>` for layout, you must add `import { Column } from '@tale-ui/react/column';` — `Column` is not globally available (see the `Column` pitfall below).** **`ColorPicker.Root` itself requires `@internationalized/color`** — its `value` prop accepts a `Color` object and you need `parseColor` to create the initial value. You must run `npm install @internationalized/color` as a **separate prerequisite step before writing any code** that uses `ColorPicker.Root`, even if no standalone `ColorArea`/`ColorSlider` is involved. This is the same install-first rule as standalone compositions — see the `@internationalized/color` pitfall below. **Any code that passes a `Color` object to `ColorPicker.Root value={...}` or calls `parseColor()` will fail with "Cannot find module '@internationalized/color'" if the package is not installed first.** **This applies even when ColorPicker.Root only wraps other components like ColorArea — the ColorPicker context itself requires the Color type and parseColor function, so the import error occurs regardless of what is nested inside.** **`ColorArea`, `ColorSwatch`, `ColorSlider`, and other color components are NOT exported from `@tale-ui/react/color-picker`** — each has its own separate import path: `import { ColorArea } from '@tale-ui/react/color-area'`, `import { ColorSwatch } from '@tale-ui/react/color-swatch'`, `import { ColorSlider } from '@tale-ui/react/color-slider'`. Importing any of these from `@tale-ui/react/color-picker` will cause "has no exported member" TypeScript errors. Additionally, **`ColorPicker.Root`'s `value` prop requires a `Color` object, not a plain CSS string** — `useState('#3b82f6')` (string) is incompatible; use `useState(parseColor('#3b82f6'))` (requires `npm install @internationalized/color`). **`ColorPicker.Root` does NOT accept a `label` prop** — passing `label="…"` causes 'is not assignable to type ColorPickerProps' TypeScript errors. `ColorPicker.Root` only accepts `value`, `onChange`, and `children`. If you need a visible label, place a `<ColorField.Label>` or plain `<Text>` element outside the `ColorPicker.Root` wrapper.
   - **`parseDate`, `DateValue`, `RangeValue`, and other date utilities must NOT be imported from `@internationalized/date` or `react-aria-components` directly** — these are internal dependencies of Tale UI and are **not** available in consumer projects. For `RangeCalendar` and `Calendar`, you do not need to import `DateValue` or `RangeValue` at all for basic usage: for controlled state (passing `value` and `onChange` to `RangeCalendar.Root`), you **must** annotate the state variable with the correct types — `useState(null)` with no type annotation causes TypeScript to infer the literal `null` type, making `setValue` typed as `Dispatch<SetStateAction<null>>` which is incompatible with `onChange`'s expected `(value: RangeValue<DateValue>) => void` signature and produces 'Type 'Dispatch<SetStateAction<null>>' is not assignable to type '(value: RangeValue<DateValue>) => void'' TypeScript errors. The correct controlled pattern derives the value type directly from the component's own `onChange` prop to avoid any third-party import: `type RangeValueType = Parameters<NonNullable<React.ComponentProps<typeof RangeCalendar.Root>['onChange']>>[0]; const [value, setValue] = useState<RangeValueType | null>(null);` (also add `import * as React from 'react';` if `React` is not already in scope as a namespace, and always add `import { useState } from 'react';` separately — `useState` is NOT available via the `React` namespace import alone, so omitting it causes 'Cannot find name useState' TypeScript errors). If you wrap the calendar and any output text in a `<Column>` for layout, you must also add `import { Column } from '@tale-ui/react/column';` — `Column` is never globally available and omitting its import causes 'Cannot find name Column' TypeScript errors. **`RangeValue` and `DateValue` are NOT exported from `@tale-ui/react/range-calendar`** — importing them from that module causes 'has no exported member' TypeScript errors. Neither `RangeValue` nor `DateValue` are re-exported from any `@tale-ui/react/*` module — do **NOT** write `import type { RangeValue, DateValue } from '@tale-ui/react/calendar'`; doing so causes 'has no exported member' TypeScript errors for both types. For controlled `RangeCalendar` state, derive the value type directly from the component's own `onChange` prop to avoid any third-party import: `type RangeValueType = Parameters<NonNullable<React.ComponentProps<typeof RangeCalendar.Root>['onChange']>>[0]; const [value, setValue] = useState<RangeValueType | null>(null);` (also add `import * as React from 'react';` if `React` is not already in scope as a namespace). **`RangeCalendar` has NO `Footer` sub-part** — using `<RangeCalendar.Footer>` causes 'Property 'Footer' does not exist' TypeScript errors. The only available sub-parts are: `Root`, `Header`, `PreviousButton`, `Heading`, `NextButton`, `Grid`, `GridHeader`, `GridHeaderCell`, `GridBody`, and `Cell`. To display the selected date range as text, place a `<Text color="muted">` element *outside* `RangeCalendar.Root` rather than inventing a Footer sub-part. **Critical: never use the native JavaScript `Date` type with `RangeCalendar`** — `useState<[Date, Date] | null>` is incompatible with `RangeCalendar.Root`'s `value` and `onChange` props (which expect `RangeValue<DateValue>`, not a tuple of native `Date` objects) and causes 'Type '[Date, Date] | null' is not assignable to type 'RangeValue<DateValue> | null | undefined'' TypeScript errors. Do **not** import `parseDate` from `@internationalized/date` unless you have explicitly installed it as a direct dependency (`npm install @internationalized/date`) — missing this package causes 'Cannot find module' TypeScript errors. **`RangeCalendar.Root` does NOT accept a `locale` prop** — passing `locale="en-US"` (or any locale string) directly to `RangeCalendar.Root` causes 'is not assignable to type RangeCalendarProps' TypeScript errors because that prop does not exist on this component. The same applies to `Calendar.Root`. For locale and RTL support, wrap your component tree in `<I18nProvider locale="en-US">` from `@tale-ui/react/i18n-provider` instead.
   - **`Autocomplete` with static items needs NO external imports** — for a fixed list, use `Autocomplete.Root` with static `Autocomplete.Item` children directly (no `useState`, no `useFilter`). The built-in filtering works automatically. Only add `useFilter` from `react-aria` when you need dynamic client-side filtering driven by a controlled input value (e.g. filtering an array from props or state). **Do NOT import `useFilter` or `react-aria` for basic static Autocomplete usage** — the module is not installed by default in consumer projects and will cause "Cannot find module 'react-aria'" TypeScript errors. When `useFilter` IS genuinely needed, import it as: `import { useFilter } from 'react-aria';` and tell the consumer to run `npm install react-aria` as a separate prerequisite step before writing any code.
   - **`@internationalized/color` is only needed for controlled (`useState`) color state — NOT for uncontrolled (`defaultValue`) usage.** `ColorWheel`, `ColorArea`, `ColorSlider`, `ColorField`, `ColorPicker`, and `ColorSwatchPicker` all accept a plain CSS color string for `defaultValue` (e.g. `defaultValue="hsl(0, 100%, 50%)"`). **When using `ColorWheel`, `ColorArea`, and `ColorSlider` with controlled state (e.g. `const [color, setColor] = useState<Color>(parseColor('hsl(0, 100%, 50%)'))` and `value={color} onChange={setColor}`), you must install `@internationalized/color` as a prerequisite** — the `Color` type and `parseColor` function are required for the state variable, and missing this package causes "Cannot find module '@internationalized/color'" TypeScript errors. **Do NOT add `import { parseColor } from '@internationalized/color'` for basic uncontrolled usage** — the package is not installed by default in consumer projects and adding that import will cause "Cannot find module '@internationalized/color'" TypeScript errors. Only import `parseColor` and `Color` when you genuinely need a controlled `useState<Color>` variable (e.g. `const [color, setColor] = useState(parseColor('hsl(0, 100%, 50%)'))`) — and in that case, tell the consumer to run `npm install @internationalized/color` as a separate prerequisite step before writing any code. **`ColorSwatch` always accepts a plain CSS string** (`<ColorSwatch color="#3b82f6" />`) — never use `parseColor` with it unless it is driven by a shared `Color` state variable, in which case serialise: `<ColorSwatch color={color.toString('css')} />`.
   - **`ColorSwatch` `color` prop** accepts a plain CSS color string — NOT a `Color` object, and NOT `value`. The correct prop name is `color`: `<ColorSwatch color="#3b82f6" />`. **`ColorSwatch` has NO `size` prop** — passing `size="l"` (or any size value) causes 'is not assignable to type ColorSwatchProps' TypeScript errors. `ColorSwatch` only accepts `color` and `className`; control its dimensions via CSS or a wrapping element if needed. This applies inside `ColorSwatchPicker.Item` too — `ColorSwatchPicker.Item` takes a `color` prop for selection tracking, but the `ColorSwatch` child inside it also requires its own `color` prop: `<ColorSwatchPicker.Item color="#3b82f6"><ColorSwatch color="#3b82f6" /></ColorSwatchPicker.Item>`. Do NOT use `value` on `ColorSwatch` — it has no `value` prop and doing so causes a TypeScript error. When `ColorSwatch` is driven by shared color state (a `Color` object from `useState`), you must serialise it: `<ColorSwatch color={color.toString('css')} />`. Passing the raw `Color` object causes a TypeScript error because `ColorSwatchProps.color` is typed as `string`. This is a common mistake when composing `ColorSwatch` alongside `ColorArea`, `ColorSlider`, `ColorWheel`, `ColorPicker`, or `ColorSwatchPicker` — always call `.toString('css')` on the state value before passing to `ColorSwatch`.
   - **`DateRangePicker` uses `StartDate` and `EndDate` sub-parts, NOT `DateInput`** — there is no `DateRangePicker.DateInput` export; using it causes "Property 'DateInput' does not exist" TypeScript errors. The two date segment inputs are `DateRangePicker.StartDate` and `DateRangePicker.EndDate`, each requiring a render function as children: `{(segment) => <DateRangePicker.Segment segment={segment} />}`. Additionally, `DateRangePicker.Dialog` does NOT auto-render a calendar — you must compose a full `RangeCalendar` inside it (imported from `@tale-ui/react/range-calendar`). `DateRangePicker.Trigger` is self-closing. Example: `<DateRangePicker.Root><DateRangePicker.Label>Date range</DateRangePicker.Label><DateRangePicker.Group><DateRangePicker.StartDate>{(segment) => <DateRangePicker.Segment segment={segment} />}</DateRangePicker.StartDate><span aria-hidden="true">&ndash;</span><DateRangePicker.EndDate>{(segment) => <DateRangePicker.Segment segment={segment} />}</DateRangePicker.EndDate><DateRangePicker.Trigger /></DateRangePicker.Group><DateRangePicker.Popover><DateRangePicker.Dialog><RangeCalendar.Root><RangeCalendar.Header><RangeCalendar.PreviousButton /><RangeCalendar.Heading /><RangeCalendar.NextButton /></RangeCalendar.Header><RangeCalendar.Grid><RangeCalendar.GridHeader>{(day) => <RangeCalendar.GridHeaderCell>{day}</RangeCalendar.GridHeaderCell>}</RangeCalendar.GridHeader><RangeCalendar.GridBody>{(date) => <RangeCalendar.Cell date={date} />}</RangeCalendar.GridBody></RangeCalendar.Grid></RangeCalendar.Root></DateRangePicker.Dialog></DateRangePicker.Popover></DateRangePicker.Root>`. Also add `import { RangeCalendar } from '@tale-ui/react/range-calendar';`.
   - `PaymentInput.CardIcon` auto-detects card type — use self-closing inside `PaymentInput.Group`. `PaymentInput.Input` auto-formats digits — do not manually format values.
   - `Avatar.Group` propagates size to children — do not set size on individual `Avatar.Root` inside a Group unless overriding.
   - `Avatar.Indicator` must wrap **outside** `Avatar.Root` (not inside it) because `.tale-avatar` has `overflow: hidden`. Pass the badge element via the `badge` prop. `Avatar.LabelGroup` propagates size to child avatars like `Avatar.Group`. **There is no `AvatarLabelGroup` component** — use a simple flex layout with `Avatar.Root`, `Avatar.Image`, `Avatar.Fallback`, and separate `Text` components for labels instead.
   - **`CheckboxGroup` and `Checkbox` have separate import paths — do NOT cross-import.** `CheckboxGroup` is exported from `@tale-ui/react/checkbox-group` (NOT from `@tale-ui/react/checkbox`), and `Checkbox` is exported from `@tale-ui/react/checkbox` (NOT from `@tale-ui/react/checkbox-group`). Cross-importing causes 'Module has no exported member' TypeScript errors. Always use two separate imports: `import { CheckboxGroup } from '@tale-ui/react/checkbox-group';` and `import { Checkbox } from '@tale-ui/react/checkbox';`. `Checkbox.Indicator` does NOT render a checkmark on its own — you must provide a child `<Icon icon={Check} size="sm" />` from `@tale-ui/react/icon` and `lucide-react`.
   - **Never use `.Visual` exports** (`Checkbox.Visual`, `Radio.Visual`, `Switch.Visual`, `ToggleButtonVisual`) for interactive UI. They are `aria-hidden` building blocks for custom component composition only — not for application screens.
   - `Dialog.Close` and `Popover.Close` auto-render an X icon — use them self-closing (`<Dialog.Close aria-label="Close" />`). Pass children only to override the default icon.
   - `Dialog.Backdrop` must **wrap** `Dialog.Popup` (opposite of Drawer).
   - **`Dialog` has no `Header` or `Body` sub-parts** — place `Dialog.Title` and `Dialog.Description` directly inside `Dialog.Popup`. Do NOT use `Dialog.Header` or `Dialog.Body` — they do not exist and will cause TypeScript errors. The correct structure is: `<Dialog.Popup><Dialog.Close aria-label="Close" /><Dialog.Title>…</Dialog.Title><Dialog.Description>…</Dialog.Description><Dialog.Actions>…</Dialog.Actions></Dialog.Popup>`.
   - **`Row` `justify` prop** only accepts values defined in the `Justify` type — `'space-between'` is NOT valid. Use `justify="between"` instead (Tale UI uses shorthand token names, not CSS property values). **`Row` does NOT accept `alignItems`, `flexDirection`, `flexWrap`, or other raw CSS flex properties as props** — passing `alignItems="center"` causes 'is not assignable to type RowProps' TypeScript errors. If you need to control cross-axis alignment, pass an inline `style` or `className` instead: `<Row gap="s" style={{ alignItems: 'center' }}>` or wrap content in a `<div style={{ display: 'flex', alignItems: 'center', gap: 'var(--space-s)' }}>`. The only layout-specific props Row exposes are `gap` (token scale: `'4xs'`–`'4xl'`) and `justify` (shorthand tokens like `'between'`, `'center'`, `'end'`, `'start'`).
   - `Select.Root` owns the `placeholder` prop — do NOT put `placeholder` on `Select.Value`. **`Select.Root` controlled-state uses `selectedKey` and `onSelectionChange`** — do NOT use `value`, `onValueChange`, or `onChange` on `Select.Root`; those props do not exist and cause "is not assignable to type" TypeScript errors. Example: `<Select.Root selectedKey={selected} onSelectionChange={(key) => setSelected(key as string)}>`.
   - `Combobox.Input` owns the `placeholder` prop — do NOT put `placeholder` on `Combobox.Root`. **`Combobox.Root` controlled-state props are `selectedKey` and `onSelectionChange`** — do NOT use `value` or `onValueChange` on `Combobox.Root`; those props do not exist and cause "is not assignable to type" TypeScript errors. For controlled input text, use `inputValue` and `onInputChange`. Example: `const [selected, setSelected] = useState<string | null>(null); ... <Combobox.Root selectedKey={selected} onSelectionChange={(key) => setSelected(key as string | null)}><Combobox.Label>Country</Combobox.Label><Combobox.InputGroup><Combobox.Input placeholder="Select a country" /><Combobox.Trigger /></Combobox.InputGroup><Combobox.Popover><Combobox.ListBox>{['Apple', 'Banana', 'Cherry'].map(item => <Combobox.Item key={item} id={item}>{item}</Combobox.Item>)}</Combobox.ListBox></Combobox.Popover></Combobox.Root>`. **Always define item data inline (e.g. `const items = ['Apple', 'Banana', 'Cherry']` in the same file) — never import list data from a local module like `'./country-data'` that does not exist in the consumer project; this causes "Cannot find module" TypeScript errors.**
   - **`Accordion` requires `Accordion.Trigger` inside `Accordion.Header`** — `Accordion.Header` is the `<h3>` heading element; it does not render an interactive button on its own. Always nest `Accordion.Trigger` inside it: `<Accordion.Header><Accordion.Trigger>Title</Accordion.Trigger></Accordion.Header>`. Placing text directly inside `<Accordion.Header>` (without `Accordion.Trigger`) produces non-interactive items that cannot be expanded or collapsed. **`Accordion` uses `Accordion.Panel` for content, NOT `Accordion.Content`** — there is no `Content` sub-part on the `Accordion` namespace. **`Accordion.Root` does NOT accept a `type` prop** — passing `type="single"` or `type="multiple"` causes 'Type '{ children: Element[]; type: string; }' is not assignable to type AccordionRootProps' TypeScript errors because `type` does not exist on `AccordionRootProps`. Omit the `type` prop entirely. The correct structure is: `<Accordion.Root><Accordion.Item id="x"><Accordion.Header><Accordion.Trigger>Title</Accordion.Trigger></Accordion.Header><Accordion.Panel><Text>Content here</Text></Accordion.Panel></Accordion.Item></Accordion.Root>`. Using `<Accordion.Content>` will cause "Property 'Content' does not exist" TypeScript errors. **`Accordion.Root` uses `defaultExpandedKeys` (NOT `defaultSelectedKey`, `defaultValue`, or `defaultOpen`) for setting the initially expanded item(s)** — `defaultValue` is a common React/HTML pattern but does NOT exist on `AccordionRootProps`; using it causes 'is not assignable to type AccordionRootProps' TypeScript errors. **`Banner` is a namespace component — always use `<Banner.Root>`, never `<Banner>` directly** — using `<Banner>` directly causes "'Banner' cannot be used as a JSX component" and "does not have any construct or call signatures" TypeScript errors. `Banner.Root` accepts a `variant` prop (`'error'`, `'warning'`, `'success'`) and arbitrary children. There is NO `'brand'` variant, NO `'neutral'` variant, and NO `'info'` variant — using `variant="brand"`, `variant="neutral"`, or `variant="info"` causes 'Type '"neutral"' is not assignable to type Variant | undefined' (and similar) TypeScript errors. The ONLY valid `variant` values are `'error'`, `'warning'`, and `'success'`. Omit the `variant` prop entirely for the default/informational style. For an informational or brand-colored banner, omit `variant` rather than guessing a non-existent value. **`Column` is missing from the generated code when the layout wraps components in a vertical stack** — whenever a vertical stack of components (heading + accordion + banner, or similar) is composed, wrap them in `<Column gap="..." >` and add `import { Column } from '@tale-ui/react/column';` instead of using a bare `<div className="...">` wrapper. Pass content directly as children (e.g. `<Text>` or inline JSX). Example: `<Banner.Root variant="success"><Text>Operation completed successfully.</Text></Banner.Root>` or without a variant for the default style: `<Banner.Root><Text>Still have questions? <Link href="#contact">Contact us</Link></Text></Banner.Root>`. Import: `import { Banner } from '@tale-ui/react/banner';` — `defaultSelectedKey` is a React Aria `Select`/`Tabs` prop and causes 'is not assignable to type AccordionRootProps' TypeScript errors when used on `Accordion.Root`. For uncontrolled usage with a default open item, use `defaultExpandedKeys={new Set(["item-id"])}`. For controlled state, use `expandedKeys` and `onExpandedChange`. Example: `<Accordion.Root type="single" defaultExpandedKeys={new Set(["q1"])}>`.
   - **`TextArea.TextArea` sub-part is PascalCase** — the textarea input sub-part is `TextArea.TextArea`, NOT `TextArea.Textarea` (lowercase 'a'). Using `<TextArea.Textarea>` will cause "Property 'Textarea' does not exist" TypeScript errors. Correct usage: `<TextArea.Root><TextArea.Label>Bio</TextArea.Label><TextArea.TextArea placeholder="Tell us about yourself" /></TextArea.Root>`.
   - **There is NO `PasswordInput` component in Tale UI** — `import { PasswordInput } from '@tale-ui/react'` causes 'has no exported member PasswordInput' TypeScript errors because the component does not exist. For password fields, use `TextField` with `type="password"` on `TextField.Input`: `<TextField.Root isRequired><TextField.Label>Password</TextField.Label><TextField.Input name="password" type="password" placeholder="••••••••" /></TextField.Root>`. The `type` attribute is placed on `TextField.Input`, not `TextField.Root` (see the `TextField.Root` `name` pitfall below).
   - **`Form` `method` prop requires lowercase** — the valid values are `"get"`, `"post"`, and `"dialog"`. Using uppercase `"POST"` or `"GET"` causes 'Type '"POST"' is not assignable to type '"dialog" | "get" | "post" | undefined'' TypeScript errors. Always use lowercase: `<Form method="post" action="/login">`.
   - **`TextField.Root` does NOT accept a `name` prop** — only `isRequired`, `isInvalid`, `isDisabled`, and `isReadOnly` are valid on `TextField.Root`. The `name` prop (and HTML input attributes like `type`, `minLength`, `maxLength`, `placeholder`) must be placed on `TextField.Input`: `<TextField.Root isRequired><TextField.Label>Email</TextField.Label><TextField.Input name="email" type="email" placeholder="you@example.com" /></TextField.Root>`. Placing `name` on `TextField.Root` causes "No overload matches this call" TypeScript errors. The same rule applies to `TextArea.Root` — put `name` on `TextArea.Textarea`, and to `NumberField.Root` — put `name` on `NumberField.Input`.
   - `Combobox.Trigger` auto-renders a chevron icon — use it self-closing inside `Combobox.InputGroup`.
   - `SearchField.ClearButton` does NOT auto-render an icon — pass `<Icon icon={X} size="sm" />` as children (import from `@tale-ui/react/icon` and `lucide-react`).
   - **`SearchField` has no `InputGroup` sub-part** — `SearchField.Input` and `SearchField.ClearButton` are direct children of `SearchField.Root`. Do NOT wrap them in `SearchField.InputGroup` — it does not exist and will cause a TypeScript error. Correct structure: `<SearchField.Root><SearchField.Label>…</SearchField.Label><SearchField.Input placeholder="…" /><SearchField.ClearButton><Icon icon={X} size="sm" /></SearchField.ClearButton></SearchField.Root>`.
   - **`Card` sub-parts and content pattern** — `Card.Root` wraps `Card.Header`, `Card.Body`, and optionally `Card.Footer`. **`Card.Root` has NO `elevated` boolean prop** — to render the elevated card style use `variant="elevated"`: `<Card.Root variant="elevated">`. Passing `elevated` as a boolean prop causes 'is not assignable to type RootProps' TypeScript errors. **`Card.Header` has NO `title` or `description` props** — pass children directly: `<Card.Header><Text variant="heading" size="s">Profile</Text></Card.Header>`. Using `<Card.Header title="Profile" />` will NOT render any visible text — `title` silently becomes an HTML tooltip attribute on the wrapper `<div>`. `Card.Body` and `Card.Footer` accept arbitrary children. **`Card.Body` and `Card.Footer` do NOT accept a `gap` prop** — passing `gap="m"` (or any spacing token) to `Card.Body` causes 'is not assignable to type BodyProps' TypeScript errors. To space out children inside a `Card.Body`, wrap them in a `<Column gap="m">` instead: `<Card.Body><Column gap="m"><TextField.Root>…</TextField.Root><TextField.Root>…</TextField.Root></Column></Card.Body>`. **Do NOT use inline `marginTop`, `marginBottom`, or any other spacing styles on children inside `Card.Body` to create vertical gaps** — the correct pattern is always a `<Column gap="...">` wrapper around all Card.Body children, regardless of whether you tried to pass `gap` to `Card.Body` directly. This applies to any mix of children (e.g. `Text`, `Row`, `Badge`, `TextField`). Similarly, for `Card.Footer` horizontal layouts, wrap children in a `<Row gap="...">` instead of applying `style={{ display: 'flex', gap: '...' }}` directly on `Card.Footer`. Both `Column` and `Row` require their own explicit imports: `import { Column } from '@tale-ui/react/column';` and `import { Row } from '@tale-ui/react/row';`. Example: `<Card.Root><Card.Header><Text variant="heading" size="s">Profile</Text></Card.Header><Card.Body><TextField.Root><TextField.Label>Name</TextField.Label><TextField.Input placeholder="Jane Doe" /></TextField.Root></Card.Body></Card.Root>`. When placing multiple cards side by side, wrap each in a `<Column>` inside a `<Row>`: `<Row gap="l"><Column><Card.Root>…</Card.Root></Column><Column><Card.Root>…</Card.Root></Column></Row>`.
   - **`Select` required sub-part structure** — `Select.Root` must contain `Select.Trigger` (which wraps `Select.Value`) and `Select.Popover` (which wraps `Select.ListBox` containing `Select.Item` children). **There is no `Select.Option`** — using `<Select.Option>` will cause "Property 'Option' does not exist" TypeScript errors. Use `Select.Item` instead. A minimal Select: `<Select.Root placeholder="Pick one"><Select.Label>Theme</Select.Label><Select.Trigger><Select.Value /></Select.Trigger><Select.Popover><Select.ListBox><Select.Item id="light">Light</Select.Item><Select.Item id="dark">Dark</Select.Item></Select.ListBox></Select.Popover></Select.Root>`. Do NOT use `<option>` HTML elements or `<Select.Option>` — use `Select.Item`. Do NOT omit `Select.Trigger` — `Select.Value` must be wrapped in it.
   - **`Badge` `variant` prop** accepts semantic variants (`'neutral'`, `'brand'`, `'error'`, `'warning'`, `'success'`) and color variants (`'red'`, `'orange'`, `'amber'`, `'yellow'`, `'lime'`, `'green'`, `'emerald'`, `'teal'`, `'cyan'`, `'sky'`, `'indigo'`, `'violet'`, `'purple'`, `'fuchsia'`, `'pink'`, `'rose'`). Note that `'danger'` is **not** a valid variant and will cause TypeScript errors — use `'error'` instead. `Badge` is a simple component — pass `variant` and children text: `<Badge variant="success">Active</Badge>`. Do NOT use a `color` prop — Badge uses `variant` for color.
   - **`Button` `variant` prop** accepts `'primary'`, `'neutral'`, `'danger'`, `'ghost'`, or `'link'` — use `variant="ghost"` for a ghost button and `variant="primary"` for a primary button. `Button` is a simple component (no `.Root`). Example: `<Button variant="ghost">Cancel</Button><Button variant="primary">Save</Button>`.
   - **`Breadcrumbs.Link` is the correct link sub-part inside `Breadcrumbs.Item`** — do NOT use the standalone `Link` component from `@tale-ui/react/link` inside breadcrumb items. `Breadcrumbs.Link` is a dedicated sub-part that applies the correct breadcrumb link styles and separator punctuation automatically. Correct structure: `<Breadcrumbs.Item><Breadcrumbs.Link href="#">Home</Breadcrumbs.Link></Breadcrumbs.Item>`. Using `<Link>` instead of `<Breadcrumbs.Link>` will produce unstyled links without breadcrumb separators.
   - **`NavigationMenu` is the correct component for site navigation bars — there is NO `Nav` component** — importing `import { Nav } from '@tale-ui/react/nav'` will cause "Cannot find module" TypeScript errors. The correct import is `import { NavigationMenu } from '@tale-ui/react/navigation-menu'`. Use `NavigationMenu.Link` (not the standalone `Link` component) for nav links. **`NavigationMenu.List` is required between `NavigationMenu.Root` and `NavigationMenu.Item`** — `NavigationMenu.List` renders the `<ul>` that provides the flex layout for nav items. Placing `NavigationMenu.Item` directly inside `NavigationMenu.Root` skips this wrapper and breaks the layout. Correct structure: `<NavigationMenu.Root><NavigationMenu.List><NavigationMenu.Item><NavigationMenu.Link href="#">Home</NavigationMenu.Link></NavigationMenu.Item></NavigationMenu.List></NavigationMenu.Root>`.
   - **`Menu` requires `Menu.MenuList` between `Menu.Popover` and `Menu.Item`** — `Menu.MenuList` renders the menu container that holds menu items. Placing `Menu.Item` directly inside `Menu.Popover` (without `Menu.MenuList`) will cause "Property 'List' does not exist" TypeScript errors. The correct structure is: `<Menu.Root><Menu.Trigger className="tale-button tale-button--neutral tale-button--md">File</Menu.Trigger><Menu.Popover><Menu.MenuList><Menu.Item id="new" onAction={() => console.log('New')}>New</Menu.Item></Menu.MenuList></Menu.Popover></Menu.Root>`. To group items with a separator, use multiple `Menu.MenuList` elements separated by `Menu.Separator` — `Menu.Separator` must be a sibling of `Menu.MenuList`, NOT placed inside a `Menu.MenuList`. Example: `<Menu.Popover><Menu.MenuList><Menu.Item id="new">New</Menu.Item><Menu.Item id="open">Open</Menu.Item></Menu.MenuList><Menu.Separator /><Menu.MenuList><Menu.Item id="save">Save</Menu.Item></Menu.MenuList><Menu.Separator /><Menu.MenuList><Menu.Item id="close">Close</Menu.Item></Menu.MenuList></Menu.Popover>`. Do NOT omit `Menu.MenuList` — it is required for proper menu rendering and layout. **Every group of `Menu.Item` elements must be wrapped in its own `Menu.MenuList`** — this is mandatory even for a single item in a group, and the absence of `Menu.MenuList` causes "Property 'List' does not exist" TypeScript errors on `Menu`. **A complete grouped menu with a trigger button follows this exact pattern:** `<Menu.Root><Menu.Trigger className="tale-button tale-button--neutral tale-button--md">File</Menu.Trigger><Menu.Popover><Menu.MenuList><Menu.Item id="new" onAction={() => console.log('New')}>New</Menu.Item><Menu.Item id="open" onAction={() => console.log('Open')}>Open</Menu.Item></Menu.MenuList><Menu.Separator /><Menu.MenuList><Menu.Item id="save" onAction={() => console.log('Save')}>Save</Menu.Item></Menu.MenuList><Menu.Separator /><Menu.MenuList><Menu.Item id="close" onAction={() => console.log('Close')}>Close</Menu.Item></Menu.MenuList></Menu.Popover></Menu.Root>` — note that `Menu.Trigger` uses `className` (not `asChild` or nested `<Button>`) and every group of items is wrapped in its own `Menu.MenuList`.

   - **`Menu.Root` does not accept `onAction`, `open`, or `onOpenChange`** — `Menu.Root` wraps React Aria's `MenuTrigger` and only accepts `MenuTrigger` props (e.g. `isDisabled`). It manages its own open/close state internally — do NOT add `useState` to control the menu and do NOT pass `open={isOpen}` or `onOpenChange={setIsOpen}` to `Menu.Root`; doing so causes 'is not assignable to type MenuTriggerProps' TypeScript errors that cascade into false 'Property does not exist' errors on all `Menu.*` sub-parts (including `Menu.MenuList`, `Menu.Popover`, etc.). Place `onAction` on each `Menu.Item` individually: `<Menu.Item id="save" onAction={() => handleSave()}>Save</Menu.Item>`. There is no single top-level action handler on the root or list.

   - **Trigger button styling** — Triggers render their own `<button>`, so never nest `<Button>` inside them and never use `asChild` — the `asChild` prop does not exist on any Tale UI trigger (`Menu.Trigger`, `Popover.Trigger`, `Dialog.Trigger`, `Tooltip.Trigger`, `AlertDialog.Trigger`, `Drawer.Trigger`, `PreviewCard.Trigger`) and causes 'is not assignable to type' TypeScript errors. Style the trigger directly with `className` instead. They differ in how BEM classes are applied:

     | Trigger                                                | Auto-applies `tale-button`? | className you provide                                                                   |
     | ------------------------------------------------------ | --------------------------- | --------------------------------------------------------------------------------------- |
     | `Dialog.Trigger`                                       | Yes                         | `className="tale-button--primary"` (variant only)                                       |
     | `AlertDialog.Trigger`                                  | No                          | `className="tale-button tale-button--danger"` (base + variant)                          |
     | `Drawer.Trigger`, `Drawer.Close`                       | No                          | `className="tale-button tale-button--neutral"` (base + variant)                         |
     | `Popover.Trigger`, `Tooltip.Trigger`, `Menu.Trigger`   | No                          | `className="tale-button tale-button--neutral tale-button--md"` (base + variant + size)  |
     | `PreviewCard.Trigger`                                  | No                          | `className="tale-button tale-button--neutral"` (base + variant) or style as a link      |

   - **`Tooltip` namespace structure** — `Tooltip` is a namespace component; always use `<Tooltip.Root>`, never `<Tooltip>` directly — using `<Tooltip>` causes "'Tooltip' cannot be used as a JSX component" and "does not have any construct or call signatures" TypeScript errors. `Tooltip.Trigger` does **not** support `asChild` — passing `asChild={true}` causes "is not assignable to type" TypeScript errors (same rule as all Tale UI triggers). Style `Tooltip.Trigger` directly with `className` instead of nesting a `<Button>` inside it. The tooltip text goes in `Tooltip.Popup`. Correct structure: `<Tooltip.Root><Tooltip.Trigger className="tale-button tale-button--primary tale-button--md"><Icon icon={Save} size="sm" />Save changes</Tooltip.Trigger><Tooltip.Popup>Save changes</Tooltip.Popup></Tooltip.Root>`. Import: `import { Tooltip } from '@tale-ui/react/tooltip';`.
   - **Icon and IconButton:**
     - `Icon` takes a component ref, not an instance: `<Icon icon={Heart} />` (correct) vs `<Icon icon={<Heart />} />` (wrong). Import icons from `lucide-react`.
     - **`Icon` `size` prop** uses `'sm' | 'md' | 'lg' | 'xl'` — NOT the standard Tale UI token suffixes (`'s'`, `'m'`, `'l'`). The default size is `'md'`. Example: `<Icon icon={Heart} size="md" />`.
     - `IconButton` requires `aria-label` for accessibility: `<IconButton aria-label="Delete"><Icon icon={Trash2} /></IconButton>`.
   - **`Text` component import** — always import `Text` explicitly: `import { Text } from '@tale-ui/react/text'`. `Text` is also a global Web API type (DOM); if the import is missing TypeScript resolves to that global, producing "'Text' cannot be used as a JSX component" errors. Always use `<Text>` to render text content inside components like `Card.Header`, `Card.Body`, `Card.Footer`, and similar container parts — do not place raw strings directly as children without a `<Text>` wrapper.
   - **`Text` component `size` prop** only accepts `'xs'`, `'s'`, `'m'` (default), or `'l'`. Values like `'2xl'`, `'xl'`, `'lg'`, etc. are **not valid** and will cause TypeScript errors. To render large display numbers or headings, use the `variant` prop (e.g. `variant="display"` or `variant="heading"`) — do NOT set `size` beyond `'l'`.
   - **`Text` monospace variant is `"mono"`, NOT `"code"`** — passing `variant="code"` causes 'is not assignable to type Variant' TypeScript errors. For monospace/code text use `variant="mono"`. Valid variants: `'display'`, `'heading'`, `'title'`, `'label'`, `'text'`, `'mono'`. **`Text` does NOT accept a `weight` prop** — passing `weight="bold"` or any weight value causes 'is not assignable to type TextProps' TypeScript errors. Font weight is determined automatically by `variant` (e.g. `display` and `heading` are bold). **`Text` component `color` prop** only accepts `'default'` (default), `'muted'` (renders in `--neutral-60`), or `'accent'` (renders in `--color-60`). There is no `'secondary'`, `'tertiary'`, or other color value. To render muted/secondary text use `color="muted"`.
   - **`Text` component `as` prop (`TextElement`)** only accepts: `'p'`, `'span'`, `'div'`, `'h1'`–`'h6'`, `'label'`. It does **not** accept `'strong'`, `'code'`, `'pre'`, `'em'`, `'small'`, `'li'`, `'dt'`, `'dd'`, or any other HTML elements outside this list — using them causes a TypeScript error (`Type '"strong"' is not assignable to type 'TextElement | undefined'`). To render bold/strong text, use a native `<strong>` element directly (e.g. `<strong>Label</strong>`) — do NOT use `<Text as="strong">`. To render inline code, use a native `<code>` element or `<Text as="span">` with `variant="mono"`. This error is especially common inside `PreviewCard.Content` where developers try to use `<Text as="strong">` for the card title — always use a bare `<strong>` tag instead.
   - **`Text` component has NO `weight` prop.** There is no `weight`, `fontWeight`, or `bold` prop on `Text`. To render bold or semibold text, wrap the content in a native `<strong>` element — do NOT use `as="strong"` because `'strong'` is not a valid `TextElement` value. Do not pass `weight="semibold"` or any weight value — it will cause a TypeScript error.
   - **Never annotate return types as `JSX.Element` or use `React.FC`** — this project uses the new JSX transform (`react-jsx`), so the `JSX` namespace is not available without an explicit import. Do NOT write `function Foo(): JSX.Element`, `const Foo: React.FC = ...`, or `import React from 'react'`. Write plain functions with no return type annotation: `export function Foo() { return (...); }`. Using `JSX.Element` or `React.FC` causes "Cannot find namespace 'JSX'" TypeScript errors.
   - **Token size suffixes:** Tale UI uses `-s`, `-m`, `-l` (NOT Bootstrap-style `-sm`, `-md`, `-lg`). Full scale: `4xs, 3xs, 2xs, xs, s, m, l, xl, 2xl, 3xl, 4xl`. Example: `--space-m` not `--space-md`.
   - **Layout utilities:** Tale UI provides layout utility classes (`.flex--row`, `.flex--col`, `.gap--m`, `.grid--3`, `.center--hv`, etc.) — use these instead of writing manual `display: flex` / `gap` declarations.
   - **`Column` inside `Row`:** When placing side-by-side content in a `Row`, wrap each child in a `<Column>` component (imported from `@tale-ui/react/column`) instead of applying `style={{ flex: 1 }}` or other manual flex-grow styles directly on the child. `Column` is a simple component (no `.Root`) and handles equal-width flex layout automatically. Example: `<Row gap="l"><Column><Card.Root>…</Card.Root></Column><Column><Card.Root>…</Card.Root></Column></Row>`. **Do not use a plain `<div>` with `style={{ maxWidth, margin, padding }}` as a page layout wrapper when a `<Column>` or `<Container>` component would serve the purpose** — `Container` (from `@tale-ui/react/container`) is the correct component for centering page-level content with max-width constraints. **`Column` is also the correct wrapper for a single centered/constrained content block inside a `Row` — any time a `Row` is used for layout (even with a single child), wrap children in `<Column>` rather than relying on bare `<div>` or applying flex styles directly on child elements.** Even when no `Row` is present, if you use `Column` in the component tree you must add `import { Column } from '@tale-ui/react/column';` — it is a separate package and will not be available without an explicit import. **`Column` is NOT exported from `@tale-ui/react/row`, and there is NO `@tale-ui/react/row-column` module** — do not import both `Row` and `Column` from the same path. `import { Row, Column } from '@tale-ui/react/row'` causes 'has no exported member Column' TypeScript errors; `import { Row, Column } from '@tale-ui/react/row-column'` causes 'Cannot find module' TypeScript errors because that module does not exist. Always use two completely separate imports: `import { Row } from '@tale-ui/react/row';` and `import { Column } from '@tale-ui/react/column';`. This is the most common mistake when using `Row` with `Column` children — both combined paths look plausible but are always wrong. Every file that uses `<Column>` must have its own `import { Column } from '@tale-ui/react/column';` line, completely separate from the `Row` import. **This is the single most common cause of 'Cannot find name Column' TypeScript errors** — whenever `<Column>` appears in generated JSX, verify the import line `import { Column } from '@tale-ui/react/column';` is present at the top of the file. **This rule applies even when `Column` is used only for layout centering or max-width constraints with no `Row` present — any use of `<Column>` anywhere in JSX requires its explicit import.** Do not assume `Column` is globally available or re-exported from another Tale UI package. **Conversely, when centering or constraining a single block of content (e.g. `style={{ maxWidth: 320 }}`), wrap it in a `<Column>` rather than applying `maxWidth` inline on the component itself** — this is the idiomatic Tale UI approach and requires the `Column` import. **This rule applies even when there is no `Row` at all — any top-level page section that would otherwise be wrapped in a `<div style={{ maxWidth: ..., margin: '0 auto' }}>` must use `<Container>` (for full page centering) or `<Column>` (for a constrained content block), with the corresponding import added. A plain `<div>` with `maxWidth`/`margin`/`padding` inline styles is never the correct substitute. A bare `<div>` with no styles is also not correct — even a simple page section or content group that would otherwise be a plain `<div>` wrapper should use `<Column>` to maintain consistent layout semantics.** When composing a vertical stack of components (e.g. a heading + accordion + banner), wrap them in a `<Column>` rather than a bare `<div>`.
   - **AlertDialog vs Dialog:** `AlertDialog` uses `role="alertdialog"` and is for actions requiring explicit acknowledgement (deletions, confirmations). Do NOT add `<AlertDialog.Close />` (corner X icon) — it allows dismissal without choosing an action. Force the user to pick Cancel or Confirm. For general-purpose modals that can be freely dismissed, use `Dialog` instead.
   - **`AlertDialog` sub-parts and controlled state** — `AlertDialog.Root` accepts `isOpen`/`onOpenChange` (NOT `open`/`onOpenChange`). The available sub-parts are: `Root`, `Trigger`, `Backdrop`, `Popup`, `Title`, `Description`, and `Actions`. There is NO `AlertDialog.Cancel` or `AlertDialog.Action` sub-part — place plain `<Button>` components directly inside `AlertDialog.Actions` for the cancel and confirm buttons. `AlertDialog.Trigger` does NOT support `asChild` — style it with `className="tale-button tale-button--danger"` (see trigger button styling table). When managing open state with `useState`, always import it: `import { useState } from 'react'`. Correct structure: `<AlertDialog.Root isOpen={isOpen} onOpenChange={setIsOpen}><AlertDialog.Trigger className="tale-button tale-button--danger">Delete Account</AlertDialog.Trigger><AlertDialog.Backdrop /><AlertDialog.Popup><AlertDialog.Title>Delete Account?</AlertDialog.Title><AlertDialog.Description><Text color="muted">This action is permanent and cannot be undone.</Text></AlertDialog.Description><AlertDialog.Actions><Button variant="neutral" onPress={() => setIsOpen(false)}>Cancel</Button><Button variant="danger" onPress={handleDelete}>Delete</Button></AlertDialog.Actions></AlertDialog.Popup></AlertDialog.Root>`.
   - **I18nProvider:** For locale/RTL support, wrap your app in `<I18nProvider locale="...">` from `@tale-ui/react/i18n-provider`. This affects date/time formatting, number formatting, and text direction for all Tale UI components.
   - **CSPProvider:** If your app uses a Content Security Policy, wrap your app in `<CSPProvider nonce={myNonce}>` from `@tale-ui/react/csp-provider` to apply the nonce to Tale UI's inline styles.
   - **Do not apply global styles to semantic HTML elements.** Tale UI components internally render `<section>`, `<header>`, and other semantic elements (via React Aria). Global rules like `section { padding: 1rem }` will leak into Dialog, Menu, Popover, and other overlay components and break their layout. Style with class selectors (`.my-section { ... }`) instead of element selectors. Tale UI includes a defensive CSS shield that resets `margin`, `padding`, and `border` on semantic elements directly inside `.tale-*` containers, but nested elements are not covered.

   - **`ColorWheel` is a namespace component — always use `<ColorWheel.Root>`, never `<ColorWheel>` directly** — using `<ColorWheel>` directly will cause "'ColorWheel' cannot be used as a JSX component" and "does not have any construct or call signatures" TypeScript errors because the exported value is a namespace object, not a React component. The correct minimal usage is: `<ColorWheel.Root defaultValue="hsl(0, 100%, 50%)" outerRadius={120} innerRadius={88}><ColorWheel.Thumb /></ColorWheel.Root>`. **`ColorWheel` `outerRadius` and `innerRadius` props are required** — `ColorWheel.Root` will not render visibly without both props. Use `outerRadius={120}` and `innerRadius={88}` (or adjust to suit your layout). Do not omit them. For uncontrolled usage, pass a plain string to `defaultValue` (e.g. `defaultValue="hsl(0, 100%, 50%)"`) — this requires NO import from `@internationalized/color`. See the `@internationalized/color` pitfall above for when the package IS needed. **For controlled state, `ColorWheel.Root`'s `value` prop requires a `Color` object and `onChange` returns a `Color` object — do NOT use `useState<string>` or `useState('hsl(0, 100%, 50%)')` with `value`/`onChange`; using a string setter causes "Type 'Dispatch<SetStateAction<string>>' is not assignable to type '(value: Color) => void'" TypeScript errors. Use `const [color, setColor] = useState(parseColor('hsl(0, 100%, 50%)'))` and `value={color} onChange={setColor}` (requires `npm install @internationalized/color` as a prerequisite step).**
   - **`RatingBadge` requires a `value` prop** (number) — do NOT pass the rating as `children` text. Use `<RatingBadge value={4.8} />`, not `<RatingBadge>4.8</RatingBadge>`. Similarly, `RatingStars` requires a `value` prop.
   - **`AppStoreButton` `store` prop** accepts `'apple'` or `'google'` — NOT `'app-store'` or `'play-store'`. Use `store="apple"` for the Apple App Store and `store="google"` for the Google Play Store.
   - **`SocialButton` requires a `provider` prop** — do NOT guess prop names like `variant`, `type`, or `icon`. The `provider` prop accepts `'google'`, `'github'`, `'apple'`, `'x'`, or `'facebook'` and renders the corresponding provider icon automatically. Pass the button label as children: `<SocialButton provider="google">Sign in with Google</SocialButton>`. `SocialButton` is a simple component (no `.Root`) imported from `@tale-ui/react/social-button`. To group multiple social buttons vertically, wrap them in `<SocialButtonGroup>` (imported from `@tale-ui/react/social-button-group`).
   - **`DotIcon` `color` prop** accepts semantic tokens only: `'neutral'`, `'brand'`, `'error'`, `'warning'`, `'success'` — NOT CSS color names like `'green'`, `'red'`, or `'blue'`. For a green status dot use `color="success"`, for red use `color="error"`, for yellow/amber use `color="warning"`.
   - - **`Radio.Root` does NOT accept `checked`, `onChange`, or native HTML input props** — these are HTML `<input type="radio">` attributes incompatible with `RadioRootProps`. Selection state is managed by `RadioGroup` via its `value`/`onChange` props, not by individual `Radio.Root` components. Passing `checked` to `Radio.Root` causes 'is not assignable to type RadioRootProps' TypeScript errors. **`Radio.Group` does not exist** — there is no `Group` sub-part on the `Radio` namespace. To wrap radio buttons in a group, import `RadioGroup` separately: `import { RadioGroup } from '@tale-ui/react/radio-group'`. Then use `<RadioGroup label="Select a plan">` as the wrapper around `<Radio.Root>` children. Using `<Radio.Group>` will fail at runtime with a 'Radio.Group is not a component' error.
   - **`Tree` sub-parts are ONLY `Root`, `Item`, and `ItemContent` — there is NO `Tree.Node`, `Tree.Header`, `Tree.ExpandTrigger`, `Tree.ExpandIcon`, `Tree.ItemText`, `Tree.Children`, or any other sub-part** — the agent commonly invents these non-existent parts. `Tree.Item` is the node sub-part (not `Tree.Node`), `Tree.ItemContent` wraps the visible row content (not `Tree.ItemText`), and child items are nested directly inside their parent `Tree.Item` (not inside a `Tree.Children` wrapper). Expandability is determined automatically by React Aria when child `Tree.Item` elements are present — there is no `hasChildren` prop, no `Tree.ExpandTrigger`, and no `Tree.ExpandIcon`. `Tree.Root` requires an `aria-label` prop. Each `Tree.Item` requires an `id` prop and a `textValue` prop (used for keyboard navigation). Example: `<Tree.Root aria-label="Files"><Tree.Item id="src" textValue="src"><Tree.ItemContent>src/</Tree.ItemContent><Tree.Item id="app" textValue="App.tsx"><Tree.ItemContent>App.tsx</Tree.ItemContent></Tree.Item></Tree.Item><Tree.Item id="pkg" textValue="package.json"><Tree.ItemContent>package.json</Tree.ItemContent></Tree.Item></Tree.Root>`. Import: `import { Tree } from '@tale-ui/react/tree';`.
   - **`Table` sub-parts are `Root`, `Header`, `Column`, `Body`, `Row`, and `Cell` — there is NO `Table.ColumnHeader` or `Table.Status`** — using `<Table.ColumnHeader>` will cause "Property 'ColumnHeader' does not exist" TypeScript errors; use `<Table.Column>` for header cells instead. There is also NO `Table.Status` sub-part for rendering status badges — use a `<Badge>` component (imported from `@tale-ui/react/badge`) inside a regular `<Table.Cell>` for status indicators. `Table.Root` requires an `aria-label` prop. Example: `<Table.Root aria-label="People"><Table.Header><Table.Column isRowHeader>Name</Table.Column><Table.Column>Role</Table.Column><Table.Column>Status</Table.Column></Table.Header><Table.Body><Table.Row id="1"><Table.Cell>Jane Doe</Table.Cell><Table.Cell>Product Manager</Table.Cell><Table.Cell><Badge variant="success">Active</Badge></Table.Cell></Table.Row></Table.Body></Table.Root>`. Import: `import { Table } from '@tale-ui/react/table'; import { Badge } from '@tale-ui/react/badge';`.
   - **`TagGroup` uses `TagGroup.Tag`, NOT `TagGroup.Item`** — there is no `Item` sub-part on the `TagGroup` namespace. Use `<TagGroup.Tag id="x">Label</TagGroup.Tag>` for each tag. Also, `TagGroup.Root` does NOT accept a `label` prop — use the `<TagGroup.Label>` sub-part instead. **`TagGroup.Tag` does NOT accept an `onRemove` prop** — removal is handled by passing `onRemove` to `TagGroup.Root`; the callback receives a `Set<Key>` of removed ids. Do NOT put `onRemove` on individual `TagGroup.Tag` elements — it will cause 'is not assignable to type TagProps' TypeScript errors. For removable tags, use `<TagGroup.Root onRemove={(keys) => setTags(prev => prev.filter(t => !keys.has(t)))}>`  with no `onRemove` on the tags themselves. Correct removable structure: `<TagGroup.Root onRemove={(keys) => setTags(prev => prev.filter(t => !keys.has(t)))}><TagGroup.Label>Skills</TagGroup.Label><TagGroup.List>{tags.map(tag => <TagGroup.Tag key={tag} id={tag}>{tag}</TagGroup.Tag>)}</TagGroup.List></TagGroup.Root>`.
   - **`Switch` has no `Label` or `Indicator` sub-parts** — `Switch` only has `Root`, `Thumb`, and `Visual`. The label text is passed as a direct child of `Switch.Root` (not via a `Switch.Label` sub-part), and `Switch.Thumb` is the sliding toggle indicator (not `Switch.Indicator`). Correct usage: `<Switch.Root><Switch.Thumb />Enable notifications</Switch.Root>`. Do NOT use `<Switch.Label>` or `<Switch.Indicator>` — they do not exist and will cause TypeScript errors.
   - **`Slider` requires a specific nested structure** — `Slider.Root` must contain `Slider.Control` → `Slider.Track` → `Slider.Indicator` + `Slider.Thumb`. For a labeled slider with value output, add `Slider.Header` containing `Slider.Label` and `Slider.Output` before `Slider.Control`. `Slider.Root` accepts `defaultValue` (number or number[] for range), `minValue`, `maxValue`, and `step`. For a range slider, pass two `Slider.Thumb` elements and an array `defaultValue`. Example (single thumb): `<Slider.Root defaultValue={50} minValue={0} maxValue={100}><Slider.Header><Slider.Label>Volume</Slider.Label><Slider.Output /></Slider.Header><Slider.Control><Slider.Track><Slider.Indicator /><Slider.Thumb /></Slider.Track></Slider.Control></Slider.Root>`. Do NOT use a bare `<Slider.Root>` without the Control/Track/Indicator/Thumb sub-parts — it will render nothing visible.
   - **`ScrollArea` requires a specific nested structure** — `ScrollArea.Root` alone does nothing. You must compose: `<ScrollArea.Root style={{ height: N }}>` → `<ScrollArea.Viewport>` → `<ScrollArea.Content>` → your content, plus a sibling `<ScrollArea.Scrollbar orientation="vertical">` containing `<ScrollArea.Thumb />`. The `Viewport` and `Content` wrappers are mandatory — placing children directly inside `Root` will not produce a scrollable area. For horizontal scrolling add a second `<ScrollArea.Scrollbar orientation="horizontal"><ScrollArea.Thumb /></ScrollArea.Scrollbar>` and a `<ScrollArea.Corner />`. Example: `<ScrollArea.Root style={{ width: 300, height: 200 }}><ScrollArea.Viewport><ScrollArea.Content><div style={{ padding: 'var(--space-s)' }}>{/* tall content */}</div></ScrollArea.Content></ScrollArea.Viewport><ScrollArea.Scrollbar orientation="vertical"><ScrollArea.Thumb /></ScrollArea.Scrollbar></ScrollArea.Root>`.
   - **`Autocomplete` sub-parts and props** — `Autocomplete.Root` contains `Autocomplete.Input` and `Autocomplete.ListBox` with `Autocomplete.Item` children. There is NO `Autocomplete.Label`, `Autocomplete.Popover`, or `Autocomplete.Trigger` — using these will cause "Property does not exist" TypeScript errors. `Autocomplete.Root` accepts NO controlled-state props — do NOT pass `inputValue`, `onInputChange`, `selectedKey`, or `onSelectionChange` to `Autocomplete.Root`; these props do not exist on it and will cause "is not assignable to type 'RootProps'" TypeScript errors. For a basic autocomplete with filtering, use: `<Autocomplete.Root><Autocomplete.Input placeholder="Search..." /><Autocomplete.ListBox>{items.map(item => <Autocomplete.Item key={item.id} id={item.id}>{item.name}</Autocomplete.Item>)}</Autocomplete.ListBox></Autocomplete.Root>`. Do NOT wrap the ListBox in a Popover — it renders as an overlay automatically.
   - **`Avatar` namespace structure and usage** — `Avatar.Root` wraps `Avatar.Image` and `Avatar.Fallback`. For user profiles, place the avatar inside content alongside separate text elements for name/role — Avatar itself only renders the circular image/initials, not text labels. Example: `<div style={{ display: 'flex', alignItems: 'center', gap: 'var(--space-s)' }}><Avatar.Root><Avatar.Image src="/user.jpg" alt="Jane Doe" /><Avatar.Fallback>JD</Avatar.Fallback></Avatar.Root><div><Text variant="heading" size="s">Jane Doe</Text><Text color="muted">Product Manager</Text></div></div>`. **`Avatar.Root` `size` prop accepts `'xs'`, `'sm'`, `'md'`, `'lg'`, `'xl'`, `'2xl'`** — these use the `sm`/`md`/`lg` abbreviation style, NOT the short `'s'`/`'m'`/`'l'` form used by some other components. Using `size="s"`, `size="m"`, or `size="l"` causes 'Type '"s"' is not assignable to type 'Size | undefined'' (and similar) TypeScript errors — use `size="sm"` for small, `size="md"` for medium, `size="lg"` for large. `Avatar.Image` requires `src` and `alt` props. `Avatar.Fallback` displays when the image fails to load — pass initials as children.
   - **`Avatar.Label` and `Avatar.LabelGroup` are NOT valid sub-parts** — there is no `Avatar.Label`, `Avatar.LabelGroup`, or `AvatarLabelGroup` component. To display user name and role beside an avatar, use `<Avatar.Root>` with a separate flex layout containing the avatar and text elements explicitly. Example: `<Row gap="s" style={{ alignItems: 'center' }}><Avatar.Root><Avatar.Image src="https://i.pravatar.cc/150?img=32" alt="Sarah Chen" /><Avatar.Fallback>SC</Avatar.Fallback></Avatar.Root><Column><Text variant="heading" size="s">Sarah Chen</Text><Text color="muted">Product Designer</Text></Column></Row>`. Using `<Avatar.LabelGroup>` or `<Avatar.Label>` causes 'Property does not exist' TypeScript errors.
   - **`DatePicker` has no Calendar sub-parts** — `DatePicker` only contains `Root`, `Label`, `Group`, `DateInput`, `Segment`, `Trigger`, `Popover`, and `Dialog`. There is NO `DatePicker.Calendar`, `DatePicker.Header`, `DatePicker.CalendarGrid`, `DatePicker.GridHeader`, `DatePicker.GridHeaderCell`, `DatePicker.GridBody`, `DatePicker.Cell`, `DatePicker.Button`, `DatePicker.PreviousButton`, `DatePicker.NextButton`, `DatePicker.Heading`, or other calendar-related sub-parts. The calendar UI is rendered automatically by `DatePicker.Dialog` — do not try to compose it manually with Calendar sub-parts or by nesting a `<Calendar.Root>` inside `DatePicker.Dialog`. Using `<DatePicker.Calendar>`, `<DatePicker.Header>`, `<DatePicker.CalendarGrid>`, or any Calendar.* sub-parts inside a DatePicker will cause "Property does not exist" TypeScript errors. **`DatePicker.Dialog` is self-closing** — render it as `<DatePicker.Dialog />` with no children; it auto-renders the calendar UI internally. **Do NOT nest a `<Calendar.Root>` or any `Calendar.*` sub-parts inside `DatePicker.Dialog`** — the calendar is rendered automatically and adding a manual `Calendar` causes duplicate/broken UI. Conversely, do NOT omit `DatePicker.Dialog` from the popover — it is required for the calendar to appear. The correct minimal popover structure is: `<DatePicker.Popover><DatePicker.Dialog /></DatePicker.Popover>` (self-closing `DatePicker.Dialog` with no children, no `Calendar` import needed). **do NOT import `DateValue` from `@tale-ui/react/date-picker`** — it is not exported from that module and will cause "Module has no exported member 'DateValue'" TypeScript errors. For controlled state, **do NOT use `useState(null)` without a type annotation** — TypeScript infers the literal `null` type, making `setSelectedDate` typed as `Dispatch<SetStateAction<null>>` which is incompatible with `onChange`'s expected `(value: DateValue | null) => void` and produces 'Type 'Dispatch<SetStateAction<null>>' is not assignable to type '(value: DateValue | null) => void'' TypeScript errors. The correct controlled pattern derives the value type from the component's own `onChange` prop (see prop-derived pattern below). **`DateValue` is NOT reliably exported from `@tale-ui/react/calendar`, and `DateValueType` is NOT exported from `@tale-ui/react/calendar` at all — importing either causes 'Module has no exported member' TypeScript errors. Always prefer deriving the value type from the component's own `onChange` prop instead of importing `DateValue` or `DateValueType`**: `type DateValueType = Parameters<NonNullable<React.ComponentProps<typeof DatePicker.Root>['onChange']>>[0]; const [selectedDate, setSelectedDate] = useState<DateValueType | null>(null);` (also add `import * as React from 'react';` if `React` is not already in scope as a namespace, and always add `import { useState } from 'react';` separately). Do NOT write `import type { DateValue } from '@tale-ui/react/calendar'`, `import type { DateValue } from '@tale-ui/react/date-picker'`, or `import { type DateValue } from 'react'` — all three cause 'Module has no exported member "DateValue"' TypeScript errors (`DateValue` is not part of React's API and is not re-exported from any `@tale-ui/react/*` module). **`DatePicker.Root` does NOT accept a `label` prop** — passing `label="…"` causes 'is not assignable to type DatePickerProps' TypeScript errors; use the `DatePicker.Label` sub-part instead. **Do NOT import `parseDate` from `@internationalized/date` for `DatePicker` controlled state** — unlike the color components (`ColorArea`, `ColorSlider`, `ColorWheel`), `DatePicker` state uses `DateValue` (not `parseDate`/`Color`) and `@internationalized/date` is never needed for date picker usage. The prop-derived pattern is always safe and requires no third-party import. **Note: if you see a TypeScript error 'Module "@tale-ui/react/calendar" has no exported member "DateValue"', this confirms `DateValue` is not re-exported from that module — use the prop-derived pattern above.** **This same rule applies when the initial state is typed as `null` without annotation in any component file** — always annotate the `useState` call with the explicit type `useState<DateValueType | null>(null)` using the prop-derived pattern above (no import needed). **When displaying the selected date as text, always call `.toString()` on the `DateValue` object — TypeScript infers the state as `never` when `useState(null)` is used without annotation, causing 'Property 'toString' does not exist on type 'never'' errors. The fix is proper annotation: `const [selectedDate, setSelectedDate] = useState<DateValue | null>(null);` and then `selectedDate.toString()` is valid.** For uncontrolled usage where you do not need to read the selected value in state, omit `value` and `onChange` entirely. Example (controlled): `import * as React from 'react'; import { useState } from 'react'; type DateValueType = Parameters<NonNullable<React.ComponentProps<typeof DatePicker.Root>['onChange']>>[0]; const [selectedDate, setSelectedDate] = useState<DateValueType | null>(null); ... <DatePicker.Root value={selectedDate} onChange={setSelectedDate}><DatePicker.Label>Select Date</DatePicker.Label><DatePicker.Group><DatePicker.DateInput>{(segment) => <DatePicker.Segment segment={segment} />}</DatePicker.DateInput><DatePicker.Trigger /></DatePicker.Group><DatePicker.Popover><DatePicker.Dialog /></DatePicker.Popover></DatePicker.Root>{selectedDate && <Text color="muted">Selected: {selectedDate.toString()}</Text>}`. Use the prop-derived pattern (`Parameters<NonNullable<React.ComponentProps<typeof DatePicker.Root>['onChange']>>[0]`) as the primary pattern — it avoids all import errors. Do NOT use `useState(null)` without a type annotation — TypeScript infers the literal `null` type, causing both 'Type Dispatch<SetStateAction<null>> is not assignable to type (value: DateValue | null) => void' and 'Property toString does not exist on type never' TypeScript errors. **Critical**: When using `useState` with `DatePicker`, **never use the native JavaScript `Date` type** — it is incompatible with `DatePicker.Root`'s `value` and `onChange` props. Do NOT write `useState<Date | null>(null)` — this causes "Type 'Date | null' is not assignable to type 'DateValue | null | undefined'" TypeScript errors. Instead, use `useState<DateValueType | null>(null)` where `DateValueType` is derived via the prop pattern: `type DateValueType = Parameters<NonNullable<React.ComponentProps<typeof DatePicker.Root>['onChange']>>[0];` (no import needed).
   - **`Carousel.Root` does NOT accept `selectedIndex`, `onSelectedIndexChange`, `currentSlide`, or `onSlideChange` props** — carousel state is managed internally. Do NOT add controlled-state props. Valid props are `loop`, `autoplay`, `slidesPerView`, and `orientation`. **`Carousel` sub-parts** — `Carousel.Root` contains `Carousel.Content` with `Carousel.Item` children, `Carousel.Indicators` containing `Carousel.Indicator` elements, and `Carousel.PreviousTrigger` / `Carousel.NextTrigger` for navigation buttons. There is NO `Carousel.Viewport`, `Carousel.PreviousButton`, `Carousel.NextButton`, or `Carousel.Previous`/`Carousel.Next` — use `Carousel.PreviousTrigger` and `Carousel.NextTrigger` (self-closing) for navigation. **Never use `document.querySelector` or imperative DOM methods to control the carousel** — the trigger sub-parts handle navigation automatically. Example: `<Carousel.Root loop><Carousel.Content><Carousel.Item>Slide 1</Carousel.Item><Carousel.Item>Slide 2</Carousel.Item><Carousel.Item>Slide 3</Carousel.Item></Carousel.Content><Carousel.PreviousTrigger /><Carousel.NextTrigger /><Carousel.Indicators><Carousel.Indicator index={0} /><Carousel.Indicator index={1} /><Carousel.Indicator index={2} /></Carousel.Indicators></Carousel.Root>`.
   - **`ColorSwatchPicker` is a namespace component — always use `<ColorSwatchPicker.Root>`, never `<ColorSwatchPicker>` directly** — using `<ColorSwatchPicker>` directly will cause "'ColorSwatchPicker' cannot be used as a JSX component" and "does not have any construct or call signatures" TypeScript errors because the exported value is a namespace object, not a React component. **`ColorSwatch` is NOT exported from `@tale-ui/react/color-swatch-picker`** — it must be imported separately: `import { ColorSwatch } from '@tale-ui/react/color-swatch'`. Importing `ColorSwatch` from `@tale-ui/react/color-swatch-picker` causes "has no exported member 'ColorSwatch'" TypeScript errors. **`ColorSwatchPicker.Root`'s `value` and `onSelectionChange` props require a `Color` object, NOT a plain CSS string** — passing `useState('#3b82f6')` (string) causes "is not assignable to type ColorSwatchPickerProps" TypeScript errors because `value` expects a `Color` object and `onSelectionChange` returns a `Color` object. For uncontrolled usage **without a change callback**, use `defaultValue` with a plain string (no `@internationalized/color` needed): `<ColorSwatchPicker.Root defaultValue="#3b82f6">`. **Never pass `value={undefined}` or `onSelectionChange={undefined}` as placeholder values** — these props do not accept `undefined` and passing them causes 'is not assignable to type ColorSwatchPickerProps' TypeScript errors. For uncontrolled usage, simply omit `value` and `onSelectionChange` from the JSX entirely. **However, `onSelectionChange` always receives a `Color` object even when `defaultValue` is used** — TypeScript will infer the parameter as `any` and then reject the callback type (`(color: any) => void` is not assignable). If you need to react to selection changes, you must install `@internationalized/color` (`npm install @internationalized/color`) and import the `Color` type to annotate the parameter: `import type { Color } from '@internationalized/color';` then `onSelectionChange={(color: Color) => console.log(color.toString('css'))}`. Alternatively, omit `onSelectionChange` entirely for purely uncontrolled display-only usage. For controlled usage, use `parseColor` from `@internationalized/color` (requires `npm install @internationalized/color` as a prerequisite): `const [color, setColor] = useState(parseColor('#3b82f6')); ... <ColorSwatchPicker.Root value={color} onSelectionChange={setColor}>`. **`ColorSwatchPicker.Root`'s `value` prop does NOT accept `null` or `undefined`** — do NOT use `useState<Color | null>(...)` or `useState<Color | undefined>(undefined)`; the `value` type is `Color` (non-nullable), so using `Color | null` or `Color | undefined` both cause 'is not assignable to type ColorSwatchPickerProps' TypeScript errors. Always initialize with a concrete color: `const [color, setColor] = useState(parseColor('#3b82f6'));` (let TypeScript infer `Color`, not `Color | null` or `Color | undefined`). There is NO `ColorSwatchPicker.List` sub-part. Each `ColorSwatchPicker.Item` requires a `color` prop (plain CSS string) and should contain a `<ColorSwatch color={sameColor} />` child. Example (uncontrolled): `import { ColorSwatchPicker } from '@tale-ui/react/color-swatch-picker'; import { ColorSwatch } from '@tale-ui/react/color-swatch'; ... <ColorSwatchPicker.Root defaultValue="#ef4444"><ColorSwatchPicker.Item color="#ef4444"><ColorSwatch color="#ef4444" /></ColorSwatchPicker.Item><ColorSwatchPicker.Item color="#3b82f6"><ColorSwatch color="#3b82f6" /></ColorSwatchPicker.Item></ColorSwatchPicker.Root>`.
   - `ContextMenu.Root` uses `open`/`onOpenChange` (NOT `isOpen`) — like `Drawer.Root`, it follows a different prop naming convention than Dialog/AlertDialog which use `isOpen`.
   - **`ContextMenu` uses `ContextMenu.Popup` (NOT `ContextMenu.Popover`) and requires `ContextMenu.MenuList` between `ContextMenu.Popup` and `ContextMenu.Item`** — there is no `Popover` sub-part and no standalone `MenuItem` import. Menu items are `ContextMenu.Item`, imported exclusively from `@tale-ui/react/context-menu`. Do NOT import `MenuItem` from `@tale-ui/react/menu` — it does not exist there and will cause "has no exported member" TypeScript errors. Correct structure: `<ContextMenu.Root><ContextMenu.Trigger>Right-click me</ContextMenu.Trigger><ContextMenu.Popup><ContextMenu.MenuList><ContextMenu.Item id="edit" onAction={() => console.log('Edit')}>Edit</ContextMenu.Item><ContextMenu.Item id="copy" onAction={() => console.log('Copy')}>Copy</ContextMenu.Item><ContextMenu.Item id="delete" onAction={() => console.log('Delete')}>Delete</ContextMenu.Item></ContextMenu.MenuList></ContextMenu.Popup></ContextMenu.Root>`.
   - **`Calendar` vs `DatePicker` — choose the right component:** Use `Calendar` when the prompt asks for a standalone calendar grid, inline calendar, or "calendar picker" for date selection (no input field, no popover — just the calendar widget itself). Use `DatePicker` when the prompt asks for a date input field with a calendar popup/dropdown. A prompt that says "show a calendar" or "date calendar picker" means the standalone `Calendar` component, NOT `DatePicker`.
   - **`DateField` vs `DatePicker` — choose the right component:** Use `DateField` when the prompt asks for a date input field, date input segments, or entering a date inline (month/day/year fields without a calendar popup). Use `DatePicker` only when a calendar picker button or popover is explicitly required. A prompt like "show a date input field with a label" means `DateField`, NOT `DatePicker`. Using `DatePicker` for a plain date input introduces an unnecessary calendar UI and leads to the `DateValue` import error (see `DatePicker` pitfall below). **`DateField` namespace structure** — `DateField.Root` contains `DateField.Label` and `DateField.DateInput` (which requires a render function with `DateField.Segment`). There is NO `DateField.Input` — the input sub-part is specifically `DateField.DateInput`. Do NOT add `useState` or any type annotation for `DateField` state — `DateField` is uncontrolled by default. Example: `<DateField.Root><DateField.Label>Enter date</DateField.Label><DateField.DateInput>{(segment) => <DateField.Segment segment={segment} />}</DateField.DateInput></DateField.Root>`. Using `<DateField>` directly or `<DateField.Input>` will cause TypeScript errors.
   - **`TimeField` vs `TimePicker` — `TimeField` is a simple inline time input, NOT a picker with a popover** — `TimeField` only has `Root`, `Label`, `DateInput`, and `Segment` sub-parts. There is NO `TimeField.Group`, `TimeField.Trigger`, `TimeField.Input`, `TimeField.Popover`, or `TimeField.Dialog` — using any of these causes 'Property does not exist' TypeScript errors. The input sub-part is `TimeField.DateInput` (same as `DateField`), which requires a render function child: `{(segment) => <TimeField.Segment segment={segment} />}`. **Do NOT use `useState<{ hours: number; minutes: number } | null>` for TimeField state** — the correct value type is `TimeValue`, but you should not import it; instead use plain `useState(null)` with no type annotation and let TypeScript infer the correct type from `TimeField.Root`'s props. Example: `<TimeField.Root><TimeField.Label>Select a time</TimeField.Label><TimeField.DateInput>{(segment) => <TimeField.Segment segment={segment} />}</TimeField.DateInput></TimeField.Root>`. For controlled state: `const [time, setTime] = useState(null); ... <TimeField.Root value={time} onChange={setTime}>`.
   - **`Drawer.Root` does NOT accept a `placement` prop** — passing `placement="right"` or any placement string causes 'is not assignable to type RootProps' TypeScript errors. The drawer slides from the end edge automatically; placement is not configurable via props. **`Drawer` basic structure and imports** — `Drawer` is a namespace component imported from `@tale-ui/react/drawer`. Basic structure: `<Drawer.Root open={isOpen} onOpenChange={setIsOpen}><Drawer.Trigger><Button>Open</Button></Drawer.Trigger><Drawer.Backdrop /><Drawer.Popup><Drawer.Close aria-label="Close" /><Drawer.Title>Settings</Drawer.Title><Drawer.Description>Configure your preferences</Drawer.Description>{/* content */}</Drawer.Popup></Drawer.Root>`. The `Drawer.Backdrop` and `Drawer.Popup` are siblings (not nested), and `Drawer.Root` manages the open state with `open`/`onOpenChange` props.
   - **`EmptyState` namespace structure** — `EmptyState.Root` contains `EmptyState.Icon`, `EmptyState.Title`, and `EmptyState.Description`. There is NO `EmptyState.Header` or `EmptyState.Content` — place title and description directly inside `EmptyState.Root`. `EmptyState.Icon` can contain an `<Icon>` component or be omitted. Example: `<EmptyState.Root><EmptyState.Icon><Icon icon={Search} size="lg" /></EmptyState.Icon><EmptyState.Title>No results</EmptyState.Title><EmptyState.Description>Try adjusting your search terms or filters</EmptyState.Description></EmptyState.Root>`. Using `<EmptyState>` directly will cause TypeScript errors.
   - **`Fieldset` only has `Root` and `Legend` sub-parts — there is NO `Fieldset.Label`, `Fieldset.Description`, `Fieldset.Caption`, or any other sub-part** — using `<Fieldset.Label>` or `<Fieldset.Description>` will cause 'Property does not exist' TypeScript errors. The `Fieldset.Legend` sub-part renders the `<legend>` element that labels the group. If you need descriptive text below the legend, place a plain `<Text>` element (from `@tale-ui/react/text`) as a direct child of `Fieldset.Root`. Correct structure: `<Fieldset.Root><Fieldset.Legend>Shipping Address</Fieldset.Legend><Text color="muted">Enter your shipping address</Text><TextField.Root><TextField.Label>Street</TextField.Label><TextField.Input placeholder="123 Main St" /></TextField.Root></Fieldset.Root>`.
   - **`Field.Root` does NOT accept validation state props** — `Field.Root` is a plain `div` wrapper (`HTMLAttributes<HTMLDivElement>`) and does not accept `isInvalid`, `isRequired`, `isDisabled`, or `isReadOnly`. Passing these to `Field.Root` causes 'is not assignable to type' TypeScript errors. Place validation state props on the inner control instead (e.g., `TextField.Root isInvalid`). `Field.Error` becomes visible when the inner control reports an invalid state. Correct structure: `<Field.Root><Field.Label>Email</Field.Label><Field.Description>We'll never share your email.</Field.Description><TextField.Root isRequired isInvalid><TextField.Input placeholder="you@example.com" /></TextField.Root><Field.Error>Please enter a valid email address.</Field.Error></Field.Root>`. **When composing `Field.Root` with `TextField.Root` inside it, both must be imported separately** — `Field` from `@tale-ui/react/field` and `TextField` from `@tale-ui/react/text-field`. Missing either import causes 'Cannot find name' TypeScript errors. For a simple field without Tale UI's validation features, use a plain `<input className="tale-input">` inside `<Field.Control>` — this requires only the `Field` import.
   - **`FileTrigger` is a simple component** — use `<FileTrigger>` directly with `onSelect` prop to handle file selection. Pass a `<Button>` or other trigger element as children. There is NO `FileTrigger.Root` — using it will cause "Property 'Root' does not exist" TypeScript errors. Example: `<FileTrigger onSelect={(fileList) => { if (fileList && fileList.length > 0) { console.log(fileList[0].name); } }}><Button>Choose File</Button></FileTrigger>`. The `onSelect` callback receives a `FileList | null` — **always null-check before accessing `fileList.length` or `fileList[0]`**; accessing either without a null guard causes 'fileList is possibly null' TypeScript errors.
   - **`GridList` namespace structure** — `GridList.Root` contains `GridList.Item` children for selectable grid layouts. There is NO standalone `GridList` component — using `<GridList>` directly will cause "Property 'Root' does not exist" TypeScript errors. Use `GridList.Root` with `selectionMode="multiple"` for multi-select functionality. **`GridList.Item` does NOT accept an `isSelected` prop** — passing `isSelected` causes 'is not assignable to type GridListItemProps' TypeScript errors. Selection state is managed entirely by `GridList.Root` via `selectedKeys` (a `Set` of selected ids) and `onSelectionChange` — do NOT maintain a separate `useState` array and pass `isSelected` to each item. **Do NOT add a separate `Checkbox` component inside `GridList.Item` to indicate selection** — the grid list handles selection styling automatically; adding a `Checkbox` is redundant and broken (it would require its own state and `Checkbox` is a namespace component requiring `Checkbox.Root`). For controlled selection, use `selectedKeys` and `onSelectionChange` on `GridList.Root`: `const [selected, setSelected] = useState<Set<string>>(new Set()); ... <GridList.Root selectionMode="multiple" selectedKeys={selected} onSelectionChange={(keys) => setSelected(keys as Set<string>)}>`. For uncontrolled multi-select, simply use `selectionMode="multiple"` with no additional state. Example: `<GridList.Root selectionMode="multiple"><GridList.Item id="1">Item 1</GridList.Item><GridList.Item id="2">Item 2</GridList.Item></GridList.Root>`.
   - **`Menu` requires `Menu.MenuList` between `Menu.Popover` and `Menu.Item`** — `Menu.MenuList` renders the menu container that holds menu items. Placing `Menu.Item` directly inside `Menu.Popover` (without `Menu.MenuList`) will cause "Property 'List' does not exist" TypeScript errors. The correct structure is: `<Menu.Root><Menu.Trigger className="tale-button tale-button--neutral tale-button--md">File</Menu.Trigger><Menu.Popover><Menu.MenuList><Menu.Item id="new" onAction={() => console.log('New')}>New</Menu.Item></Menu.MenuList></Menu.Popover></Menu.Root>`. To group items with a separator, use multiple `Menu.MenuList` elements separated by `Menu.Separator` — `Menu.Separator` must be a sibling of `Menu.MenuList`, NOT placed inside a `Menu.MenuList`. Example: `<Menu.Popover><Menu.MenuList><Menu.Item id="new">New</Menu.Item><Menu.Item id="open">Open</Menu.Item></Menu.MenuList><Menu.Separator /><Menu.MenuList><Menu.Item id="save">Save</Menu.Item></Menu.MenuList><Menu.Separator /><Menu.MenuList><Menu.Item id="close">Close</Menu.Item></Menu.MenuList></Menu.Popover>`. Do NOT omit `Menu.MenuList` — it is required for proper menu rendering and layout. **Every group of `Menu.Item` elements must be wrapped in its own `Menu.MenuList`** — this is mandatory even for a single item in a group, and the absence of `Menu.MenuList` causes "Property 'List' does not exist" TypeScript errors on `Menu`. **A complete grouped menu with a trigger button follows this exact pattern:** `<Menu.Root><Menu.Trigger className="tale-button tale-button--neutral tale-button--md">File</Menu.Trigger><Menu.Popover><Menu.MenuList><Menu.Item id="new" onAction={() => console.log('New')}>New</Menu.Item><Menu.Item id="open" onAction={() => console.log('Open')}>Open</Menu.Item></Menu.MenuList><Menu.Separator /><Menu.MenuList><Menu.Item id="save" onAction={() => console.log('Save')}>Save</Menu.Item></Menu.MenuList><Menu.Separator /><Menu.MenuList><Menu.Item id="close" onAction={() => console.log('Close')}>Close</Menu.Item></Menu.MenuList></Menu.Popover></Menu.Root>` — note that `Menu.Trigger` uses `className` (not `asChild` or nested `<Button>`) and every group of items is wrapped in its own `Menu.MenuList`.
   - **`Text` component basic usage for muted secondary text** — to create muted secondary text, use `<Text color="muted">This action cannot be undone.</Text>`. The `color="muted"` prop renders text in `--neutral-60` for secondary information. Always import `Text` explicitly: `import { Text } from '@tale-ui/react/text';`. Example: `<Text color="muted">This action cannot be undone.</Text>`.
   - **`Pagination` namespace structure** — `Pagination` is a compound namespace component that must be composed manually. Sub-parts: `Root`, `Item`, `Ellipsis`, `PreviousTrigger`, `NextTrigger`, `Dot`, `Line`. **`Pagination.Root` does NOT accept `currentPage`, `totalPages`, or `onPageChange` props** — passing these causes 'is not assignable to type RootProps' TypeScript errors. Instead, render each page button as `<Pagination.Item page={N} />` and mark the active page with the `current` prop: `<Pagination.Item page={3} current />`. Use `<Pagination.Ellipsis />` for truncated ranges and `<Pagination.PreviousTrigger />` / `<Pagination.NextTrigger />` for prev/next buttons (self-closing). Example showing page 3 of 10: `<Pagination.Root aria-label="Pagination"><Pagination.PreviousTrigger /><Pagination.Item page={1} /><Pagination.Item page={2} /><Pagination.Item page={3} current /><Pagination.Ellipsis /><Pagination.Item page={10} /><Pagination.NextTrigger /></Pagination.Root>`. For dot indicators use `<Pagination.Dot page={N} current />` and for step indicators use `<Pagination.Line page={N} current />`.
   - **`PinInput` namespace structure** — `PinInput.Root` contains `PinInput.Group` with `PinInput.Slot` children for each digit. There is NO `PinInput.Input` sub-part — using `<PinInput.Input />` will cause "Property 'Input' does not exist" TypeScript errors; the correct sub-part is `PinInput.Slot`. **`PinInput.Root` requires a `maxLength` prop** (total number of digit slots) — omitting it causes "Property 'maxLength' is missing" TypeScript errors. Each `PinInput.Slot` requires an `index` prop (zero-based). **`PinInput` has its own `PinInput.Separator` sub-part** for the visual divider between groups — do NOT use the standalone `Separator` component from `@tale-ui/react/separator`. Example for a 6-digit PIN split into two groups of 3: `<PinInput.Root maxLength={6}><PinInput.Group><PinInput.Slot index={0} /><PinInput.Slot index={1} /><PinInput.Slot index={2} /></PinInput.Group><PinInput.Separator /><PinInput.Group><PinInput.Slot index={3} /><PinInput.Slot index={4} /><PinInput.Slot index={5} /></PinInput.Group></PinInput.Root>`.
   - **API generation failures and fallback patterns** — when an AI agent encounters API errors or fails to generate code, provide a complete working example as fallback. For an info button with popover help text, use: `<Popover.Root><Popover.Trigger><IconButton aria-label="Help"><Icon icon={HelpCircle} size="sm" /></IconButton></Popover.Trigger><Popover.Popup><Text>This feature helps you manage your account settings and preferences.</Text></Popover.Popup></Popover.Root>`. Import requirements: `import { Popover } from '@tale-ui/react/popover'; import { IconButton } from '@tale-ui/react/icon-button'; import { Icon } from '@tale-ui/react/icon'; import { Text } from '@tale-ui/react/text'; import { HelpCircle } from 'lucide-react';`.
   - **`PreviewCard` namespace structure for hover previews** — `PreviewCard.Root` contains `PreviewCard.Trigger` (the hoverable element) and `PreviewCard.Content` (the popup card). The trigger can be any interactive element like a link or button. `PreviewCard.Content` contains the card title and description text. Example: `<PreviewCard.Root><PreviewCard.Trigger><Link href="#">Hover for preview</Link></PreviewCard.Trigger><PreviewCard.Content><strong>Card Title</strong><Text color="muted">This is the preview description that appears on hover.</Text></PreviewCard.Content></PreviewCard.Root>`. Import from `@tale-ui/react/preview-card`. The preview appears automatically on hover/focus of the trigger element.
   - **API generation failures and basic component fallbacks** — when an AI agent encounters API errors (500 status, moderation blocks, etc.) and generates empty code, provide complete working examples as fallback. For a primary button that says "Save", use: `<Button variant="primary">Save</Button>`. Import requirement: `import { Button } from '@tale-ui/react/button';`. `Button` is a simple component (no `.Root`) with variants: `'primary'`, `'neutral'`, `'danger'`, `'ghost'`, `'link'`. Always include the import statement to prevent "Cannot find name 'Button'" TypeScript errors.
   - **`List.Root` `variant` prop** only accepts `'plain'` (default, no separators) or `'divided'` (separator lines between items) — `'item'` is NOT a valid value and will cause 'is not assignable to type Variant' TypeScript errors. To render a divided list, use `variant="divided"`: `<List.Root variant="divided"><List.Item>Design Review</List.Item><List.Item>Sprint Planning</List.Item></List.Root>`.
   - **`Tabs` sub-parts are `Root`, `List`, `Tab`, `Panel`, and `Indicator` — there is NO `Tabs.Trigger` or `Tabs.Content`** — using `<Tabs.Trigger>` causes "Property 'Trigger' does not exist" and `<Tabs.Content>` causes "Property 'Content' does not exist" TypeScript errors. The tab button sub-part is `Tabs.Tab` (with an `id` prop), and the content area sub-part is `Tabs.Panel` (with a matching `id` prop). **`Tabs.Root` uses `defaultSelectedKey` (NOT `defaultValue`) for the initially selected tab, and `selectedKey`/`onSelectionChange` for controlled state** — passing `defaultValue` causes "is not assignable to type RootProps" TypeScript errors. Example: `<Tabs.Root defaultSelectedKey="overview"><Tabs.List><Tabs.Tab id="overview">Overview</Tabs.Tab><Tabs.Tab id="features">Features</Tabs.Tab><Tabs.Tab id="pricing">Pricing</Tabs.Tab><Tabs.Indicator /></Tabs.List><Tabs.Panel id="overview"><Text>Overview content.</Text></Tabs.Panel><Tabs.Panel id="features"><Text>Features content.</Text></Tabs.Panel><Tabs.Panel id="pricing"><Text>Pricing content.</Text></Tabs.Panel></Tabs.Root>`. `Tabs.Indicator` is an optional animated bar placed inside `Tabs.List` after the tab buttons.
   - **Use `<Image>` instead of `<img>`, and `<Row>` instead of a flex `<div>` for horizontal layouts** — `Image` (from `@tale-ui/react/image`) is a simple component and the correct replacement for native `<img>` tags in Tale UI code: `<Image src="https://i.pravatar.cc/150" alt="Sarah Chen" radius="full" />`. Do NOT write `<img src="..." style={{ borderRadius: '50%' }} />` — use `<Image>` with its `radius` prop instead. **`Image` uses `radius` (NOT `borderRadius`) for rounded corners** — passing `borderRadius` causes 'is not assignable to type ImageProps' TypeScript errors because that prop does not exist. The `radius` prop accepts `'none'`, `'sm'`, `'md'`, `'lg'`, or `'full'` — NOT the standard Tale UI token suffixes (`'s'`, `'m'`, `'l'`). Use `radius="md"` for medium rounded corners and `radius="full"` for circular images. Example: `<Image src="/photo.jpg" alt="A mountain landscape" radius="md" width={200} height={150} />`. `Row` (from `@tale-ui/react/row`) is the correct replacement for `<div style={{ display: 'flex', gap: '...' }}>` when arranging children horizontally: `<Row gap="s"><Badge variant="brand">React</Badge><Badge variant="brand">TypeScript</Badge></Row>`. Do NOT use a bare `<div style={{ display: 'flex' }}>` where `<Row>` would serve — this is a primary cause of missing `Row` and `Image` components in generated output. Whenever you would write `display: 'flex'` on a `<div>`, use `<Row>` for horizontal layout or `<Column>` for a vertical/constrained block, with their respective imports.
   - **`Menu` requires `Menu.MenuList` between `Menu.Popover` and `Menu.Item`** — `Menu.MenuList` renders the menu container that holds menu items. Placing `Menu.Item` directly inside `Menu.Popover` (without `Menu.MenuList`) will cause "Property 'List' does not exist" TypeScript errors. The correct structure is: `<Menu.Root><Menu.Trigger className="tale-button tale-button--neutral tale-button--md">File</Menu.Trigger><Menu.Popover><Menu.MenuList><Menu.Item id="new" onAction={() => console.log('New')}>New</Menu.Item></Menu.MenuList></Menu.Popover></Menu.Root>`. To group items with a separator, use multiple `Menu.MenuList` elements separated by `Menu.Separator` — `Menu.Separator` must be a sibling of `Menu.MenuList`, NOT placed inside a `Menu.MenuList`. **Every group of `Menu.Item` elements must be wrapped in its own `Menu.MenuList`** — this is mandatory even for a single item in a group. Example with grouped items: `<Menu.Popover><Menu.MenuList><Menu.Item id="new">New</Menu.Item><Menu.Item id="open">Open</Menu.Item></Menu.MenuList><Menu.Separator /><Menu.MenuList><Menu.Item id="save">Save</Menu.Item></Menu.MenuList><Menu.Separator /><Menu.MenuList><Menu.Item id="close">Close</Menu.Item></Menu.MenuList></Menu.Popover>`.
6. **Charts (separate package):** Install `@tale-ui/charts` and `recharts` separately. Import chart styles via `import '@tale-ui/charts/styles';`. Charts use the same compound parts pattern: `BarChart.Root`, `BarChart.Bar`, etc.

8. **A2UI protocol support (optional):** If this project uses AI agents that render UI via the [A2UI protocol](https://a2ui.org/), install `@tale-ui/a2ui`. It maps A2UI agent messages to Tale UI components. See `node_modules/@tale-ui/a2ui/README.md` or the [integration guide](https://github.com/Tale-UI/core/blob/main/docs/a2ui-integration.md). Quick setup:

   ```tsx
   import { A2UIProvider, A2UISurface } from '@tale-ui/a2ui/renderer';
   import { taleUICatalog } from '@tale-ui/a2ui/catalog';

   <A2UIProvider catalog={taleUICatalog} onAction={handleAction}>
     <A2UISurface surfaceId="main" />
   </A2UIProvider>
   ```

   The catalog covers 135 standard A2UI types (Text, Button, Row, Column, Card, Image, List, TextInput, Checkbox, CheckboxGroup, Radio, Select, SelectNative, Switch, Table, TableHeader, TableColumn, TableBody, TableRow, TableCell, Tabs, TabList, TabItem, TabPanel, Badge, Progress, Spinner, Separator, Link, Icon, Form, Avatar, Banner, Disclosure, TextAreaInput, NumberInput, SliderInput, SearchInput, Accordion, Menu, Dialog, AlertDialog, Drawer, Tooltip, TooltipTrigger, TooltipPopup, Popover, PopoverTrigger, PopoverPopup, Breadcrumbs, BreadcrumbItem, BreadcrumbLink, Pagination, PaginationItem, PaginationPrevious, PaginationNext, PaginationEllipsis, TagGroup, TagList, Tag, EmptyState, Meter, ProgressCircle, ToggleButton, ToggleButtonGroup, Field, FieldLabel, FieldDescription, FieldError, Combobox, ComboboxItem, PinInput, Calendar, RangeCalendar, DateField, DatePicker, DateRangePicker, TimeField, ColorField, ColorArea, ColorSlider, PreviewCard, ContextMenu, ContextMenuTrigger, ContextMenuPopup, ContextMenuItem, ContextMenuSeparator, NavigationMenu, NavigationMenuItem, NavigationMenuLink, Carousel, CarouselItem, ScrollArea, Toolbar, ToolbarButton, ToolbarSeparator, GridList, GridListItem, Tree, TreeItem, IconButton, Fieldset, DropZone, FileTrigger, RatingStars, RatingBadge, FeaturedIcon, DotIcon, AppStoreButton, SocialButton, PaymentInput, Autocomplete, AutocompleteItem, ColorWheel, ColorSwatch, ColorSwatchPicker, ColorPicker). Extend with `createCatalog()` for custom types. Validate agent output with `validateMessages()` from `@tale-ui/a2ui/validation`.

7. **Dark mode must persist between refreshes.** Every new app must:

   a. Add `class="tale-ui"` and `data-color-mode` to `<html>`:
   ```html
   <html class="tale-ui" data-color-mode="light">
   ```

   b. Include this inline script in `<head>` before any CSS or JS to avoid a flash of wrong theme:
   ```html
   <script>
     (function() {
       var mode = localStorage.getItem('color-mode')
         || (matchMedia('(prefers-color-scheme: dark)').matches ? 'dark' : 'light');
       document.documentElement.setAttribute('data-color-mode', mode);
     })();
   </script>
   ```

   c. Use `ColorModeToggle` for the toggle UI — it handles `localStorage` and `data-color-mode` automatically:
   ```tsx
   import { ColorModeToggle } from '@tale-ui/react/color-mode-toggle';

   <ColorModeToggle />
   ```
