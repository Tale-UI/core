export enum ButtonDataAttributes {
  /**
   * Present when the button is disabled.
   */
  disabled = 'data-disabled',
  /**
   * Present when the button is in a pending/loading state.
   */
  pending = 'data-pending',
}
