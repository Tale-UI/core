{
  "slug": "file-upload-basic",
  "difficulty": "medium",
  "prompt": "Show a file upload area that accepts images and PDFs up to 5 MB, and displays uploaded files with a progress bar.",
  "reference": "import { FileUpload } from '@tale-ui/react/file-upload';\nimport { useState } from 'react';\n\nexport function UploadArea() {\n  const [files, setFiles] = useState<File[]>([]);\n  const [progress, setProgress] = useState<Record<number, number>>({});\n\n  const addFiles = (fl: FileList) => {\n    const arr = Array.from(fl);\n    setFiles((prev) => {\n      const next = [...prev, ...arr];\n      arr.forEach((_, i) => {\n        const idx = prev.length + i;\n        let pct = 0;\n        const timer = setInterval(() => {\n          pct += 10;\n          setProgress((p) => ({ ...p, [idx]: pct }));\n          if (pct >= 100) clearInterval(timer);\n        }, 200);\n      });\n      return next;\n    });\n  };\n\n  return (\n    <div style={{ maxWidth: 480 }}>\n      <FileUpload.Root>\n        <FileUpload.DropZone\n          hint=\"PNG, JPG, PDF (max 5 MB)\"\n          accept=\"image/*,.pdf\"\n          maxSize={5 * 1024 * 1024}\n          onDropFiles={addFiles}\n        />\n        {files.length > 0 && (\n          <FileUpload.List>\n            {files.map((f, i) => (\n              <FileUpload.ListItemProgressBar\n                key={`${f.name}-${i}`}\n                name={f.name}\n                size={f.size}\n                progress={progress[i] ?? 0}\n                onDelete={() => {\n                  setFiles((p) => p.filter((_, j) => j !== i));\n                  setProgress((p) => {\n                    const next = { ...p };\n                    delete next[i];\n                    return next;\n                  });\n                }}\n              />\n            ))}\n          </FileUpload.List>\n        )}\n      </FileUpload.Root>\n    </div>\n  );\n}\n",
  "tags": ["FileUpload"]
}
