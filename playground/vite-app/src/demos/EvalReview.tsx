// Placeholder — run `pnpm golden:fix-review` to generate eval results
export default function EvalReview() {
  return (
    <div style={{ padding: 'var(--space-m)', color: 'var(--neutral-60)' }}>
      No eval results yet. Run <code>pnpm golden:fix-review</code> to generate.
    </div>
  );
}
